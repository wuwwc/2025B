# 第一问算法

## 1. 算法目标

问题 1 已建立两类模型：

- **主模型：EESM + 有序阈值模型**
- **基线模型：EESM + 单调线性模型**

两种模型均以关闭 TxBF 的样本作为训练数据，前两层结构一致：

\[
H_{2\times4\times122}
\longrightarrow
\{\gamma_k\}_{k=1}^{122}
\longrightarrow
z(\beta)=\mathrm{SINR}_{\mathrm{eff}}.
\]

其中

\[
\gamma_{j,k}
=
\frac{\|H_k^{(j)}\|_F^2}{P_n^{(j)}},
\]

且固定 \(\alpha=1\)，EESM 写为

\[
\boxed{
z_j(\beta)
=
-\ln
\left[
\frac1{122}
\sum_{k=1}^{122}
\exp
\left(
-\frac{\gamma_{j,k}}{\beta}
\right)
\right]
},
\qquad \beta>0.
\]

由于数据中没有真实的等效 SINR 标签，因此不能单独拟合 \(\beta\)。本文采用 **外层搜索 EESM 参数 \(\beta\)，内层求解第三层模型参数** 的剖面优化思想。

---

# 2. 两类模型的优化类型

## 2.1 有序阈值主模型

模型为

\[
\widehat y_j
=
Q_{\boldsymbol\tau}
\left(
z_j(\beta)
\right),
\]

其中

\[
\tau_1<\tau_2<\cdots<\tau_L.
\]

联合目标为

\[
\min_{\beta>0,\;\tau_1<\cdots<\tau_L}
\frac1N
\sum_{j=1}^N
w_{c(y_j)}
\left|
c(y_j)
-
c\left[
Q_{\boldsymbol\tau}(z_j(\beta))
\right]
\right|.
\]

该问题整体属于：

\[
\boxed{
\text{带有序约束的非线性、非光滑、非凸优化问题}
}
\]

因为：

1. \(z_j(\beta)\) 关于 \(\beta\) 非线性；
2. \(Q_{\boldsymbol\tau}\) 为阶梯函数，不可微；
3. 阈值必须满足严格有序约束。

但当 \(\beta\) 固定后，问题退化为：

\[
\boxed{
\text{一维有序分割问题}
}
\]

因此可以利用动态规划精确求解最优阈值。

---

## 2.2 单调线性基线模型

模型为

\[
\widetilde c_j
=
a z_j(\beta)+b,
\qquad a\ge0.
\]

联合目标为

\[
\min_{\beta>0,\;a\ge0,\;b\in\mathbb R}
\frac1N
\sum_{j=1}^N
w_{c(y_j)}
\left[
c(y_j)-a z_j(\beta)-b
\right]^2.
\]

整体属于：

\[
\boxed{
\text{非线性最小二乘问题}
}
\]

但固定 \(\beta\) 后，

\[
z_j(\beta)
\]

全部成为已知数，此时模型退化为：

\[
\boxed{
\text{带 }a\ge0\text{ 约束的凸加权线性回归}
}
\]

因此 \(a,b\) 可以解析求解。

---

# 3. 总体求解思想

两个模型统一采用：

\[
\boxed{
\text{外层一维搜索 }\beta
+
\text{内层求最优第三层参数}
}
\]

即定义：

\[
L^*(\beta)
=
\min_{\theta}
L(\beta,\theta),
\]

其中：

- 对主模型，
  \[
  \theta=\boldsymbol\tau;
  \]
- 对基线模型，
  \[
  \theta=(a,b).
  \]

于是原来的联合优化问题被转化为：

\[
\boxed{
\widehat\beta
=
\arg\min_{\beta>0}
L^*(\beta).
}
\]

由于 \(\beta>0\)，令

\[
\theta_\beta=\ln\beta,
\]

在对数尺度上搜索，可保证正性并覆盖不同数量级。

---

# 4. 主模型求解算法

## 4.1 外层：\(\beta\) 的对数网格搜索

先构造候选集合：

\[
\mathcal B
=
\{\beta_1,\beta_2,\ldots,\beta_M\}.
\]

建议采用：

\[
\boxed{
\text{粗网格搜索}
\rightarrow
\text{最优区域局部细化}
}
\]

即先在较宽的 \(\log\beta\) 区间粗略扫描，再在表现最好的区域加密搜索。

具体搜索范围不预先固定，应先根据训练数据中

\[
\gamma_{j,k}
\]

的数值尺度确定。

---

## 4.2 固定 \(\beta\) 后计算等效 SINR

对于候选 \(\beta_q\)，计算所有训练样本：

\[
z_j(\beta_q)
=
-\ln
\left[
\frac1{122}
\sum_{k=1}^{122}
\exp
\left(
-\frac{\gamma_{j,k}}{\beta_q}
\right)
\right].
\]

为避免指数项数值下溢，程序实现时使用 `logsumexp`：

\[
\boxed{
z_j
=
-
\left[
\operatorname{logsumexp}
\left(
-\frac{\gamma_{j,1}}{\beta},
\ldots,
-\frac{\gamma_{j,122}}{\beta}
\right)
-
\ln 122
\right].
}
\]

---

## 4.3 对等效 SINR 排序

固定 \(\beta\) 后，将所有样本按

\[
z_j
\]

从小到大排列：

\[
z_{(1)}
\le
z_{(2)}
\le
\cdots
\le
z_{(N)}.
\]

同时保留对应真实类别编号：

\[
c(y_{(1)}),\ldots,c(y_{(N)}).
\]

由于模型要求

\[
\mathrm{SINR}_{\mathrm{eff}}\uparrow
\Rightarrow
\mathrm{MCS}\uparrow,
\]

所有阈值只需要设置在相邻样本之间。

候选阈值可取：

\[
\boxed{
t_i
=
\frac{
z_{(i)}+z_{(i+1)}
}{2},
\qquad i=1,\ldots,N-1.
}
\]

---

# 5. 有序阈值模型的动态规划

## 5.1 分段代价

若将排序后的第 \(i\) 到第 \(r\) 个样本全部预测为 MCS 类别 \(\ell\)，定义区间损失：

\[
\boxed{
C(i,r,\ell)
=
\sum_{s=i}^{r}
w_{c(y_{(s)})}
\left|
c(y_{(s)})-\ell
\right|.
}
\]

其中：

\[
\ell=0,1,\ldots,L.
\]

---

## 5.2 动态规划状态

定义：

\[
\boxed{
DP(\ell,i)
}
\]

表示：

> 将排序后的前 \(i\) 个样本划分到类别 \(0,1,\ldots,\ell\) 中时，可获得的最小累计有序误差。

---

## 5.3 初始条件

当只允许使用类别 \(0\) 时：

\[
\boxed{
DP(0,i)
=
C(1,i,0).
}
\]

---

## 5.4 状态转移

对于：

\[
\ell=1,\ldots,L,
\]

有

\[
\boxed{
DP(\ell,i)
=
\min_{r<i}
\left\{
DP(\ell-1,r)
+
C(r+1,i,\ell)
\right\}.
}
\]

其中：

- 前 \(r\) 个样本由类别 \(0,\ldots,\ell-1\) 划分；
- 第 \(r+1,\ldots,i\) 个样本统一预测为类别 \(\ell\)。

同时记录取得最小值的最优切分位置：

\[
r^*(\ell,i).
\]

---

## 5.5 回溯求阈值

从

\[
DP(L,N)
\]

开始，按照记录的

\[
r^*(\ell,i)
\]

反向回溯，得到各类别之间的最优切分位置：

\[
r_1,r_2,\ldots,r_L.
\]

最终阈值取为：

\[
\boxed{
\tau_\ell
=
\frac{
z_{(r_\ell)}
+
z_{(r_\ell+1)}
}{2},
\qquad
\ell=1,\ldots,L.
}
\]

由此得到固定 \(\beta\) 下的最优阈值：

\[
\boxed{
\boldsymbol\tau^*(\beta).
}
\]

---

# 6. 主模型的外层参数选择

对每个候选 \(\beta_q\)：

1. 计算训练折上的
   \[
   z_j(\beta_q);
   \]
2. 使用动态规划求
   \[
   \boldsymbol\tau^*(\beta_q);
   \]
3. 在验证折上预测 MCS；
4. 计算验证集有序 MAE：

\[
\mathrm{MAE}_{\mathrm{ord}}
=
\frac1{N_{\mathrm{val}}}
\sum_{j\in\mathrm{val}}
\left|
c(\widehat y_j)-c(y_j)
\right|.
\]

采用 \(K_{\mathrm{cv}}\)-折交叉验证：

\[
\boxed{
CV_{\mathrm{ord}}(\beta)
=
\frac1{K_{\mathrm{cv}}}
\sum_{f=1}^{K_{\mathrm{cv}}}
\mathrm{MAE}_{f,\mathrm{ord}}(\beta).
}
\]

最终：

\[
\boxed{
\widehat\beta_{\mathrm{ord}}
=
\arg\min_{\beta\in\mathcal B}
CV_{\mathrm{ord}}(\beta).
}
\]

确定 \(\widehat\beta_{\mathrm{ord}}\) 后，再使用完整训练集 A 求一次最终阈值：

\[
\boxed{
\widehat{\boldsymbol\tau}
=
\boldsymbol\tau^*
\left(
\widehat\beta_{\mathrm{ord}}
\right).
}
\]

---

# 7. 主模型算法流程

\[
\boxed{
\begin{array}{c}
\text{计算载波级 SINR}\\
\Downarrow\\
\text{选取候选 }\beta\\
\Downarrow\\
\text{计算 EESM}\\
\Downarrow\\
\text{按 }SINR_{\mathrm{eff}}\text{ 排序}\\
\Downarrow\\
\text{动态规划求最优阈值}\\
\Downarrow\\
\text{验证集计算 Ordinal MAE}\\
\Downarrow\\
\text{遍历所有 }\beta\\
\Downarrow\\
\text{选取最优 }\widehat\beta\\
\Downarrow\\
\text{全训练集重新求 }\widehat{\boldsymbol\tau}
\end{array}
}
\]

因此主模型算法可概括为：

\[
\boxed{
\text{对数网格搜索}
+
\text{动态规划}
+
K\text{-折交叉验证}.
}
\]

---

# 8. 线性基线求解算法

## 8.1 固定 \(\beta\)

对于候选 \(\beta_q\)，计算：

\[
z_j=z_j(\beta_q).
\]

此时线性模型为：

\[
c(y_j)
\approx
a z_j+b,
\qquad
a\ge0.
\]

---

## 8.2 加权均值

定义：

\[
\bar z_w
=
\frac{
\sum_{j=1}^{N}
w_jz_j
}{
\sum_{j=1}^{N}
w_j
},
\]

\[
\bar c_w
=
\frac{
\sum_{j=1}^{N}
w_jc(y_j)
}{
\sum_{j=1}^{N}
w_j
}.
\]

---

## 8.3 无约束加权最小二乘斜率

\[
\boxed{
a_0
=
\frac{
\sum_{j=1}^{N}
w_j
(z_j-\bar z_w)
(c(y_j)-\bar c_w)
}{
\sum_{j=1}^{N}
w_j
(z_j-\bar z_w)^2
}.
}
\]

---

## 8.4 加入单调约束

由于要求：

\[
a\ge0,
\]

因此最优斜率为：

\[
\boxed{
\widehat a(\beta)
=
\max(0,a_0).
}
\]

截距为：

\[
\boxed{
\widehat b(\beta)
=
\bar c_w
-
\widehat a(\beta)\bar z_w.
}
\]

因此固定 \(\beta\) 后，\(a,b\) 不需要数值优化，可以直接解析求解。

---

## 8.5 离散化得到 MCS

连续预测为：

\[
\widetilde c_j
=
\widehat a z_j+\widehat b.
\]

离散类别编号预测：

\[
\boxed{
\widehat c_j
=
\operatorname{clip}
\left(
\operatorname{round}
\left(
\widetilde c_j
\right),
0,L
\right).
}
\]

最终：

\[
\boxed{
\widehat y_j
=
m_{\widehat c_j}.
}
\]

---

# 9. 线性基线的外层参数选择

对每个候选 \(\beta_q\)：

1. 在训练折上计算
   \[
   z_j(\beta_q);
   \]
2. 解析求解
   \[
   \widehat a(\beta_q),
   \widehat b(\beta_q);
   \]
3. 在验证折上预测；
4. 计算验证集有序 MAE。

定义：

\[
\boxed{
CV_{\mathrm{lin}}(\beta)
=
\frac1{K_{\mathrm{cv}}}
\sum_{f=1}^{K_{\mathrm{cv}}}
\mathrm{MAE}_{f,\mathrm{lin}}(\beta).
}
\]

最终：

\[
\boxed{
\widehat\beta_{\mathrm{lin}}
=
\arg\min_{\beta\in\mathcal B}
CV_{\mathrm{lin}}(\beta).
}
\]

之后用全部训练集重新解析求：

\[
\boxed{
\widehat a,
\widehat b.
}
\]

---

# 10. 线性基线算法流程

\[
\boxed{
\begin{array}{c}
\text{计算载波级 SINR}\\
\Downarrow\\
\text{选取候选 }\beta\\
\Downarrow\\
\text{计算 EESM}\\
\Downarrow\\
\text{解析求解 }a,b\\
\Downarrow\\
\text{round + clip 得到 MCS}\\
\Downarrow\\
\text{验证集计算 Ordinal MAE}\\
\Downarrow\\
\text{遍历所有 }\beta\\
\Downarrow\\
\text{选取最优 }\widehat\beta\\
\Downarrow\\
\text{全训练集重新求 }\widehat a,\widehat b
\end{array}
}
\]

因此线性基线算法可概括为：

\[
\boxed{
\text{对数网格搜索}
+
\text{加权最小二乘解析解}
+
K\text{-折交叉验证}.
}
\]

---

# 11. 两种算法的统一比较

| 模型 | 外层 \(\beta\) | 内层参数 | 内层求解方式 |
|---|---|---|---|
| 有序阈值主模型 | 对数粗搜 + 局部细搜 | \(\tau_1,\ldots,\tau_L\) | 动态规划 |
| 单调线性基线 | 对数粗搜 + 局部细搜 | \(a,b\) | 加权最小二乘解析解 |

最终两种模型均使用相同指标比较：

\[
\boxed{
\mathrm{Accuracy},
\qquad
\mathrm{Ordinal\ MAE},
\qquad
\mathrm{Acc}_{\pm1}.
}
\]

其中外层选取 \(\beta\) 时统一以

\[
\boxed{
\mathrm{Ordinal\ MAE}
}
\]

作为主要优化指标。

---

# 12. 算法复杂度说明

## 12.1 主模型

设：

- 样本数为 \(N\)；
- MCS 类别数为 \(L+1\)；
- \(\beta\) 候选数为 \(M\)。

固定 \(\beta\) 后：

- 排序复杂度约为
  \[
  O(N\log N);
  \]
- 朴素动态规划约为
  \[
  O(LN^2).
  \]

因此整体复杂度约为：

\[
\boxed{
O
\left[
M
\left(
N\log N
+
LN^2
\right)
\right].
}
\]

若样本规模较大，可进一步通过前缀和、候选阈值压缩或剪枝降低计算量。

---

## 12.2 线性基线

固定 \(\beta\) 后：

- EESM 计算复杂度为
  \[
  O(122N);
  \]
- 加权线性回归为
  \[
  O(N).
  \]

因此整体近似为：

\[
\boxed{
O(122MN),
}
\]

显著低于有序阈值主模型。

---

# 13. 最终求解方案

问题 1 最终采用如下求解策略：

## 主模型

\[
\boxed{
\text{EESM}
+
\text{有序阈值模型}
}
\]

采用：

\[
\boxed{
\text{对数尺度 }\beta\text{ 粗到细搜索}
+
\text{动态规划求最优有序阈值}
+
K\text{-折交叉验证}.
}
\]

## 基线模型

\[
\boxed{
\text{EESM}
+
\text{单调线性模型}
}
\]

采用：

\[
\boxed{
\text{对数尺度 }\beta\text{ 粗到细搜索}
+
\text{加权最小二乘解析解}
+
K\text{-折交叉验证}.
}
\]

两类模型都充分利用了各自的数学结构，避免将全部参数直接交给通用黑箱优化器，从而提高求解稳定性、可解释性和计算效率。
