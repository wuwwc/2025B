# 2025 年中国研究生数学建模竞赛 B 题——问题 1 联合优化模型

## 1. 问题 1 的建模目标

问题 1 针对 **关闭 TxBF** 的 MIMO-OFDM 系统。训练集 A 给出实际信道状态信息、底噪以及实际传输速率索引 `mcs`，验证集 B 仅给出信道信息等输入变量。题目要求完成：

1. 每个载波单元的 SINR 计算；
2. 将所有载波单元的 SINR 映射为等效 SINR；
3. 建立信道传输速率模型，并对验证集 B 的 `mcs` 进行预测。

由于数据集中 **没有提供等效 SINR 的真实标签**，不能先独立拟合 EESM 参数，再单独拟合 `SINR_eff -> MCS`。因此采用 **EESM 参数与 MCS 有序阈值联合优化** 的方案。

完整模型链为

\[
H_{2\times 4\times 122}
\longrightarrow
\{\gamma_k\}_{k=1}^{122}
\longrightarrow
\gamma_{\mathrm{eff}}
\longrightarrow
\widehat{\mathrm{MCS}}.
\]

---

## 2. 符号与数据定义

设训练集中共有 \(N\) 个样本，第 \(j\) 个样本记为

\[
\left(H^{(j)},P_n^{(j)},y_j\right),\qquad j=1,\ldots,N,
\]

其中：

- \(H^{(j)}\in\mathbb C^{2\times4\times122}\)：第 \(j\) 个样本的 MIMO-OFDM 信道；
- \(H_k^{(j)}\in\mathbb C^{2\times4}\)：第 \(j\) 个样本在第 \(k\) 个载波单元上的信道矩阵；
- \(K=122\)：载波单元数；
- \(P_n^{(j)}\)：由 `noise_floor` 得到的线性噪声功率；
- \(y_j\)：训练集中的真实传输速率索引 `mcs`。

若 `noise_floor` 以 dBm 给出，则先转为线性功率。以 mW 为单位时，

\[
P_n^{(j)}
=
10^{\frac{\mathrm{noise\_floor}_j}{10}}.
\]

> 注：赛题给出了 `noise_floor` 的单位为 dBm，但没有进一步明确其与
> \(\|n_k\|^2\) 的逐天线对应方式。正式实现时必须在全数据上采用统一的噪声功率定义；
> 若将其解释为单接收支路噪声功率，则是否再乘接收天线数 \(N_r=2\) 应通过数据说明和实验验证确定，
> 不应在不同样本之间混用两种定义。

训练时只选取

\[
\mathrm{beamforming\_en}=0
\]

的样本。

---

# 3. 小问 1：每个载波单元的 SINR 模型

赛题对关闭 TxBF 的 MIMO-OFDM 系统给出的 SINR 形式为

\[
\mathrm{SINR}_k
=
\frac{\|H_k\|^2}{\|n_k\|^2}.
\]

对矩阵信道，采用 Frobenius 范数表示信道总能量：

\[
\left\|H_k^{(j)}\right\|_F^2
=
\sum_{r=0}^{1}
\sum_{c=0}^{3}
\left|h_{r,c,k}^{(j)}\right|^2.
\]

因此，第 \(j\) 个样本第 \(k\) 个载波的线性 SINR 定义为

\[
\boxed{
\gamma_{j,k}
=
\frac{
\left\|H_k^{(j)}\right\|_F^2
}{
P_n^{(j)}
}
},
\qquad
k=1,\ldots,122.
\]

从而每个样本得到一个 122 维 SINR 向量：

\[
\boxed{
\boldsymbol{\gamma}_j
=
\left(
\gamma_{j,1},
\gamma_{j,2},
\ldots,
\gamma_{j,122}
\right)
\in\mathbb R_+^{122}.
}
\]

### 注意

EESM 的指数运算应使用 **线性 SINR**，不能直接把 dB 值代入指数项。

如需展示 SINR，可另行计算

\[
\gamma_{j,k}^{(\mathrm{dB})}
=
10\log_{10}\gamma_{j,k}.
\]

但 EESM 内部仍使用 \(\gamma_{j,k}\)。

---

# 4. 小问 2：EESM 等效 SINR 模型

题目给出一般的 ESM 形式

\[
\mathrm{SINR}_{\mathrm{eff}}
=
\alpha
\phi^{-1}
\left[
\frac{1}{K}
\sum_{k=1}^{K}
\phi\left(
\frac{\mathrm{SINR}_k}{\beta}
\right)
\right].
\]

当采用指数信息度量函数

\[
\phi(x)=1-e^{-x},
\]

有

\[
\phi^{-1}(u)
=
-\ln(1-u).
\]

因此可化为

\[
\boxed{
z_j(\alpha,\beta)
=
-\alpha
\ln
\left[
\frac{1}{K}
\sum_{k=1}^{K}
\exp
\left(
-\frac{\gamma_{j,k}}{\beta}
\right)
\right]
},
\]

其中

\[
z_j=\mathrm{SINR}_{\mathrm{eff},j},
\qquad
\alpha>0,\quad \beta>0.
\]

---

## 4.1 为什么不能单独拟合 \(\alpha,\beta\)

训练数据没有提供真实的

\[
\mathrm{SINR}_{\mathrm{eff}}
\]

标签，只有最终的 `mcs` 标签。因此不能建立

\[
\mathrm{SINR}_{\mathrm{eff,true}}
\approx
z_j(\alpha,\beta)
\]

这样的独立拟合目标。

因此必须让 EESM 参数直接服务于最终的 MCS 预测，即采用

\[
(\text{EESM 参数})
+
(\text{MCS 阈值})
\]

联合优化。

---

## 4.2 \(\alpha\) 的可辨识性问题

若第三层采用有序阈值模型，则对任意 \(c>0\)，

\[
z_j' = c z_j,
\qquad
\tau_\ell'=c\tau_\ell
\]

会产生完全相同的 MCS 分类结果。

由于 \(\alpha\) 只对所有 \(z_j\) 作统一正比例缩放，因此仅依靠 MCS 标签时，

\[
\alpha
\]

与阈值尺度不可分别辨识。

因此，为保证模型参数具有唯一意义，采用归一化约束

\[
\boxed{\alpha=1}.
\]

于是问题 1 中实际需要联合估计的是

\[
\boxed{
\beta,\quad
\tau_1,\ldots,\tau_L.
}
\]

等效 SINR 简化为

\[
\boxed{
z_j(\beta)
=
-
\ln
\left[
\frac{1}{122}
\sum_{k=1}^{122}
\exp
\left(
-\frac{\gamma_{j,k}}{\beta}
\right)
\right].
}
\]

> 若比赛论文希望保留题目中的双参数记号，可以说明：
> “由于仅有 MCS 标签时 \(\alpha\) 与 MCS 阈值存在尺度不可辨识性，
> 本文固定 \(\alpha=1\) 作为尺度归一化，并联合估计 \(\beta\) 与速率阈值。”
> 这并不损失最终 MCS 预测能力。

---

# 5. 小问 3：等效 SINR 到 MCS 的有序阈值模型

## 5.1 MCS 的有序类别结构

从训练数据中提取实际出现的 MCS 值，并按从小到大排列：

\[
m_0<m_1<\cdots<m_L.
\]

这里不预先假定 MCS 一定是连续整数，而直接使用数据中实际存在的有序类别。

定义类别编号函数

\[
c(m_\ell)=\ell,
\qquad
\ell=0,\ldots,L.
\]

---

## 5.2 阈值划分

引入 \(L\) 个有序阈值：

\[
\tau_1<\tau_2<\cdots<\tau_L.
\]

并约定

\[
\tau_0=-\infty,
\qquad
\tau_{L+1}=+\infty.
\]

对于第 \(j\) 个样本，根据其等效 SINR

\[
z_j(\beta)
\]

预测 MCS：

\[
\boxed{
\widehat y_j
=
Q_{\boldsymbol\tau}
\left(z_j(\beta)\right)
=
m_\ell,
\qquad
\tau_\ell
\le
z_j(\beta)
<
\tau_{\ell+1}.
}
\]

即

\[
\widehat{\mathrm{MCS}}_j
=
\begin{cases}
m_0,
&z_j<\tau_1,\\[2mm]
m_1,
&\tau_1\le z_j<\tau_2,\\
\vdots\\
m_{L-1},
&\tau_{L-1}\le z_j<\tau_L,\\[2mm]
m_L,
&z_j\ge\tau_L.
\end{cases}
\]

该模型体现了

\[
\mathrm{SINR}_{\mathrm{eff}}\uparrow
\quad\Longrightarrow\quad
\mathrm{MCS}\uparrow
\]

的物理单调性。

---

# 6. EESM 与有序阈值的联合优化模型

## 6.1 基本联合目标

最终参数为

\[
\Theta
=
\left(
\beta,
\tau_1,\ldots,\tau_L
\right).
\]

由于 MCS 是有序类别，采用类别等级绝对误差：

\[
\ell_j
=
\left|
c(y_j)
-
c(\widehat y_j)
\right|.
\]

于是联合优化问题为

\[
\boxed{
\left(
\widehat\beta,
\widehat{\boldsymbol\tau}
\right)
=
\arg\min_{
\beta>0,\;
\tau_1<\cdots<\tau_L
}
\frac{1}{N}
\sum_{j=1}^{N}
\left|
c(y_j)
-
c\left[
Q_{\boldsymbol\tau}
\left(
z_j(\beta)
\right)
\right]
\right|.
}
\]

其中

\[
z_j(\beta)
=
-
\ln
\left[
\frac{1}{122}
\sum_{k=1}^{122}
\exp
\left(
-\frac{\gamma_{j,k}}{\beta}
\right)
\right].
\]

将三层模型代入，可写成完整形式

\[
\boxed{
\widehat y_j
=
Q_{\boldsymbol\tau}
\left\{
-
\ln
\left[
\frac{1}{122}
\sum_{k=1}^{122}
\exp
\left(
-
\frac{
\|H_k^{(j)}\|_F^2
}{
\beta P_n^{(j)}
}
\right)
\right]
\right\}.
}
\]

这就是问题 1 的最终联合模型。

---

# 7. 类别不平衡修正

如果不同 MCS 等级的样本数差异较大，则普通 MAE 会偏向高频类别。

设第 \(\ell\) 个 MCS 类别的样本数为

\[
N_\ell.
\]

可设置类别权重

\[
w_\ell
=
\frac{N}{(L+1)N_\ell}.
\]

则联合目标修改为

\[
\boxed{
\left(
\widehat\beta,
\widehat{\boldsymbol\tau}
\right)
=
\arg\min
\frac{1}{N}
\sum_{j=1}^{N}
w_{c(y_j)}
\left|
c(y_j)
-
c\left[
Q_{\boldsymbol\tau}
\left(
z_j(\beta)
\right)
\right]
\right|.
}
\]

是否采用加权版本，应根据训练集 MCS 频数分布决定。

---

# 8. 参数求解方法

由于硬阈值函数 \(Q_{\boldsymbol\tau}\) 不可微，不适合直接使用普通梯度下降。

建议采用 **外层搜索 \(\beta\) + 内层有序阈值优化**。

## 8.1 外层：搜索 EESM 参数 \(\beta\)

在对数尺度上设置候选值：

\[
\beta_1,\beta_2,\ldots,\beta_M>0.
\]

对每个候选 \(\beta_q\)：

1. 计算全部训练样本的
   \[
   z_j(\beta_q);
   \]
2. 在这些 \(z_j\) 上寻找最优有序阈值；
3. 计算交叉验证损失。

最后选择验证损失最小的

\[
\widehat\beta.
\]

实际程序中可先进行粗网格搜索，再在最优区域附近细化。

---

## 8.2 内层：优化 MCS 阈值

固定 \(\beta\) 后，将

\[
z_1,\ldots,z_N
\]

从小到大排序。

阈值只需考虑相邻有效 SINR 值之间的中点：

\[
\mathcal T
=
\left\{
\frac{z_{(i)}+z_{(i+1)}}{2}
\right\}.
\]

在满足

\[
\tau_1<\tau_2<\cdots<\tau_L
\]

的条件下，选择使有序 MAE 最小的阈值组合。

可以采用：

- 动态规划；
- 坐标下降；
- 穷举 + 剪枝；
- 连续优化后投影到有序区间。

对于本题的一维 \(z_j\)，优先推荐动态规划或坐标搜索，计算量较低且解释清晰。

---

# 9. 交叉验证与最终参数

为避免在训练集 A 上过拟合，应在 A 内部进行 \(K_{\mathrm{cv}}\)-折交叉验证。

对每组候选参数

\[
(\beta,\boldsymbol\tau)
\]

计算平均验证损失

\[
\mathrm{CVLoss}
=
\frac{1}{K_{\mathrm{cv}}}
\sum_{q=1}^{K_{\mathrm{cv}}}
L_q.
\]

最终选择

\[
\boxed{
(\widehat\beta,\widehat{\boldsymbol\tau})
=
\arg\min
\mathrm{CVLoss}.
}
\]

确定参数后，再使用训练集 A 全部样本重新拟合最终阈值。

---

# 10. 模型评价指标

由于 MCS 是有序类别，建议至少报告以下指标。

## 10.1 准确率

\[
\mathrm{Accuracy}
=
\frac{1}{N}
\sum_{j=1}^{N}
\mathbf 1(\widehat y_j=y_j).
\]

## 10.2 有序平均绝对误差

\[
\boxed{
\mathrm{MAE}_{\mathrm{ord}}
=
\frac{1}{N}
\sum_{j=1}^{N}
\left|
c(\widehat y_j)-c(y_j)
\right|.
}
\]

这是联合优化的主要指标。

## 10.3 相邻一级准确率

\[
\mathrm{Acc}_{\pm1}
=
\frac{1}{N}
\sum_{j=1}^{N}
\mathbf 1
\left(
|c(\widehat y_j)-c(y_j)|\le1
\right).
\]

该指标用于衡量预测结果是否至少落在真实 MCS 的相邻等级范围内。

---

# 11. 对验证集 B 的预测流程

对于验证集 B 中第 \(j\) 个样本：

### Step 1：恢复 122 个 MIMO 信道矩阵

\[
H_1^{(j)},\ldots,H_{122}^{(j)}.
\]

### Step 2：计算各载波 SINR

\[
\gamma_{j,k}
=
\frac{\|H_k^{(j)}\|_F^2}{P_n^{(j)}},
\qquad
k=1,\ldots,122.
\]

### Step 3：使用联合训练得到的 \(\widehat\beta\) 计算等效 SINR

\[
\boxed{
z_j
=
-
\ln
\left[
\frac{1}{122}
\sum_{k=1}^{122}
\exp
\left(
-\frac{\gamma_{j,k}}{\widehat\beta}
\right)
\right].
}
\]

### Step 4：利用最优阈值映射为 MCS

\[
\boxed{
\widehat y_j
=
Q_{\widehat{\boldsymbol\tau}}(z_j).
}
\]

最终得到验证集 B 的传输速率索引预测结果

\[
\boxed{
\widehat{\mathrm{MCS}}_1,
\widehat{\mathrm{MCS}}_2,
\ldots
}
\]

并按题目要求写入答题文档。

---

# 12. 问题 1 三个小问的整体模型总结

## 小问 1：载波级 SINR

\[
\boxed{
\gamma_{j,k}
=
\frac{
\|H_k^{(j)}\|_F^2
}{
P_n^{(j)}
}.
}
\]

输出：

\[
\boldsymbol\gamma_j
\in\mathbb R_+^{122}.
\]

---

## 小问 2：EESM 等效 SINR

尺度归一化后固定 \(\alpha=1\)，得到

\[
\boxed{
z_j(\beta)
=
-
\ln
\left[
\frac{1}{122}
\sum_{k=1}^{122}
\exp
\left(
-\frac{\gamma_{j,k}}{\beta}
\right)
\right].
}
\]

其中

\[
\beta>0
\]

不单独拟合，而与第三层 MCS 阈值联合估计。

---

## 小问 3：有序阈值速率模型

\[
\boxed{
\widehat y_j
=
Q_{\boldsymbol\tau}
\left(
z_j(\beta)
\right).
}
\]

联合求解

\[
\boxed{
\left(
\widehat\beta,
\widehat{\boldsymbol\tau}
\right)
=
\arg\min_{
\beta>0,\;
\tau_1<\cdots<\tau_L
}
\frac{1}{N}
\sum_{j=1}^{N}
w_{c(y_j)}
\left|
c(y_j)
-
c\left[
Q_{\boldsymbol\tau}
\left(
z_j(\beta)
\right)
\right]
\right|.
}
\]

因此完整链路为

\[
\boxed{
H_{2\times4\times122}
\overset{\text{小问 1}}{\longrightarrow}
\boldsymbol\gamma
\overset{\text{小问 2}}{\longrightarrow}
\mathrm{SINR}_{\mathrm{eff}}
\overset{\text{小问 3}}{\longrightarrow}
\widehat{\mathrm{MCS}}.
}
\]

其核心特点是：

\[
\boxed{
\text{不需要真实的等效 SINR 标签，而是直接利用最终 MCS 标签联合标定 EESM 参数与速率阈值。}
}
\]

---

# 13. 建模时必须进一步核实的两个数据问题

在正式代码实现前，需要先检查数据本身，因为赛题文本没有完全确定以下两点。

### 13.1 `noise_floor` 与噪声范数的对应关系

赛题给出

\[
\mathrm{SINR}_k
=
\frac{\|H_k\|^2}{\|n_k\|^2},
\]

同时数据仅给出标量 `noise_floor`。因此需要根据实际数据范围判断：

\[
P_n
\]

应解释为单接收支路噪声功率还是总噪声功率。

该选择必须固定，并通过训练集上的模型表现进行验证。

### 13.2 CSI 数值尺度

由于 `noise_floor` 使用 dBm，而 CSI 矩阵系数本身无明确物理单位说明，在正式计算前应检查 CSI 的数量级。

如果 CSI 已经经过设备内部归一化，则需要保证

\[
\frac{\|H_k\|_F^2}{P_n}
\]

处于合理数值范围；必要时应根据数据说明进行统一的尺度转换，而不能仅凭经验人为缩放单个样本。

这两项属于后续数据预处理与模型校准阶段需要解决的问题。
