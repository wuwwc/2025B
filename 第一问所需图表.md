# 第一问图表

> 本文档列出问题一正式数学建模论文中建议保留的图和表，并标明各图表在论文中的推荐出现位置、主要内容、制作方式及作用。  
> 对应论文结构参考：
>
> - 4.1 问题分析
> - 4.2 数据预处理与符号定义
> - 4.3 载波级 SINR 计算
> - 4.4 EESM 等效 SINR 模型
> - 4.5 等效 SINR 到 MCS 的速率映射
> - 4.6 联合优化与模型求解
> - 4.7 模型评价指标
> - 4.8 模型求解结果与分析
> - 4.9 验证集 B 的速率预测
> - 4.10 本问小结

---

# 一、问题一图表总览

## 1. 核心图

| 编号 | 图名 | 推荐出现位置 | 主要目的 |
|---|---|---|---|
| 图 1 | 问题一整体建模流程图 | 4.1 问题分析末尾 | 展示 CSI → SINR → EESM → 两类速率映射 → MCS 的整体逻辑 |
| 图 2 | 不同 MCS 等级的等效 SINR 分布图 | 4.8.2 等效 SINR 与 MCS 的关系 | 验证等效 SINR 与 MCS 的总体单调关系 |
| 图 3 | 等效 SINR—MCS 散点及最优有序阈值图 | 4.8.2 图 2 之后 | 直观展示有序阈值模型及各阈值位置 |
| 图 4 | EESM 参数 \(\beta\) 的交叉验证损失曲线 | 4.8.1 参数标定结果 | 展示 \(\beta\) 的选取依据与稳定性 |

## 2. 可选辅助图

| 编号 | 图名 | 推荐出现位置 | 主要目的 |
|---|---|---|---|
| 图 5 | 主模型与线性基线的预测误差分布图 | 4.8.3 主模型与基线对比 | 比较两种模型误差是否集中在 \(0,\pm1\) |
| 图 6 | 有序阈值主模型混淆矩阵 | 4.8.3 图 5 之后或附录 | 判断错误是否主要发生在相邻 MCS 等级 |

## 3. 核心表

| 编号 | 表名 | 推荐出现位置 | 主要目的 |
|---|---|---|---|
| 表 1 | 问题一主要模型参数标定结果 | 4.8.1 参数标定结果 | 给出两模型的最优 \(\beta\)、线性参数和阈值参数 |
| 表 2 | 有序阈值模型最优阈值表 | 4.8.1 表 1 之后 | 单独列出 \(\tau_1,\ldots,\tau_L\)，避免表 1 过宽 |
| 表 3 | 主模型与线性基线性能比较 | 4.8.3 主模型与基线对比 | 比较 Accuracy、Ordinal MAE、\(Acc_{\pm1}\) |
| 表 4 | 验证集 B 的最终 MCS 预测结果 | 4.9 验证集 B 的速率预测 | 给出题目要求的最终预测答案 |

## 4. 可选辅助表

| 编号 | 表名 | 推荐出现位置 | 主要目的 |
|---|---|---|---|
| 表 5 | 问题一主要符号说明 | 4.2 数据预处理与符号定义 | 集中解释 \(H,\gamma,z,\beta,\tau\) 等符号 |
| 表 6 | 问题一使用的数据字段说明 | 4.2 数据预处理部分 | 说明各原始数据字段在问题一中的用途 |

---

# 二、图 1：问题一整体建模流程图

## 出现位置

建议放在：

\[
\boxed{\text{4.1 问题分析末尾}}
\]

即完成问题一输入、输出及主要困难分析后，立即给出整体流程图。

## 图中内容

建议结构如下：

```text
              原始 CSI
         H ∈ C^(2×4×122)
                 │
                 ▼
         载波级 SINR 计算
      γ1, γ2, ..., γ122
                 │
                 ▼
           EESM 映射
         z = SINR_eff
                 │
          ┌──────┴──────┐
          │             │
          ▼             ▼
     有序阈值模型    单调线性基线
       Qτ(z)          az+b
          │             │
          └──────┬──────┘
                 ▼
             MCS 预测
```

同时可在 EESM 模块旁标出：

\[
\beta
\]

为待标定参数；在有序阈值模型旁标出

\[
\tau_1,\ldots,\tau_L;
\]

在线性基线旁标出

\[
a,b.
\]

## 画法

推荐：

- TikZ；
- draw.io；
- PowerPoint；
- Visio。

不建议用 Python 直接画流程图。

## 图注建议

> **图 1 问题一链路速率估计模型整体流程**

## 图的作用

该图用于快速说明问题一的核心思想：

\[
\boxed{
\text{物理层特征提取}
+
\text{EESM 降维}
+
\text{数据驱动速率映射}
}
\]

并明确主模型和基线模型只在第三层有所区别。

---

# 三、图 2：不同 MCS 等级的等效 SINR 分布图

## 出现位置

建议放在：

\[
\boxed{\text{4.8.2 等效 SINR 与 MCS 的关系}}
\]

作为该小节的第一张结果图。

## 图中内容

使用最终主模型得到的

\[
\widehat\beta_{\mathrm{ord}}
\]

计算全部训练样本的

\[
z_j
=
\mathrm{SINR}_{\mathrm{eff},j}.
\]

然后按照真实 MCS

\[
m_0,m_1,\ldots,m_L
\]

分组。

推荐：

\[
\boxed{\text{箱线图}}
\]

横轴：

\[
\mathrm{MCS}
\]

纵轴：

\[
\mathrm{SINR}_{\mathrm{eff}}
\]

若为了通信意义更直观，可以将等效 SINR 转成 dB 后展示：

\[
z_j^{(\mathrm{dB})}
=
10\log_{10}z_j.
\]

但模型内部仍使用线性 SINR。

## 建议显示的信息

每个 MCS 类别显示：

- 中位数；
- 上、下四分位数；
- 上下须；
- 异常点。

样本量允许时，可在箱线图上叠加透明度较低的抖动散点。

## 图注建议

> **图 2 不同 MCS 等级对应的等效 SINR 分布**

## 图的作用

用于检验：

\[
\mathrm{MCS}\uparrow
\quad\Rightarrow\quad
\mathrm{SINR}_{\mathrm{eff}}\text{总体升高}
\]

是否在数据中成立。

若各 MCS 的等效 SINR 中心位置总体右移，同时相邻等级具有一定重叠，则说明：

1. 等效 SINR 对 MCS 具有明显解释能力；
2. MCS 具有有序结构；
3. 实际关系并非完全确定的一一对应，因此需要数据驱动的阈值标定。

---

# 四、图 3：等效 SINR—MCS 散点及最优有序阈值图

## 出现位置

建议紧接图 2，仍放在：

\[
\boxed{\text{4.8.2 等效 SINR 与 MCS 的关系}}
\]

## 图中内容

横轴：

\[
z_j=\mathrm{SINR}_{\mathrm{eff},j}
\]

纵轴：

\[
c(y_j)
\]

或直接使用真实 MCS 数值。

绘制训练样本散点：

\[
(z_j,y_j).
\]

由于 MCS 为离散变量，为避免大量散点完全重合，可在纵轴仅为作图目的加入少量 jitter：

\[
y_j^{\mathrm{plot}}
=
y_j+\epsilon_j,
\qquad
\epsilon_j\in[-0.05,0.05].
\]

然后叠加：

\[
\widehat\tau_1,\ldots,\widehat\tau_L
\]

对应的竖直虚线。

进一步推荐画出最终预测阶梯函数：

\[
\widehat y
=
Q_{\widehat{\boldsymbol\tau}}(z).
\]

示意：

```text
MCS
 ↑
 5|                         ─────────
 4|                   ──────
 3|             ──────
 2|       ──────
 1| ──────
  +------|------|------|------→ SINR_eff
        τ1     τ2     τ3
```

## 图注建议

> **图 3 等效 SINR 与实际 MCS 的关系及最优有序阈值**

## 图的作用

这是证明主模型合理性的核心图。

它直接展示：

- 数据点的实际分布；
- 阈值的位置；
- 各 MCS 对应的等效 SINR 区间；
- 模型预测的阶梯式变化。

如果阈值间距明显不等，则还可以直观说明为什么简单线性模型不能充分表示

\[
\mathrm{SINR}_{\mathrm{eff}}
\rightarrow
\mathrm{MCS}
\]

之间的关系。

---

# 五、图 4：EESM 参数 \(\beta\) 的交叉验证损失曲线

## 出现位置

建议放在：

\[
\boxed{\text{4.8.1 参数标定结果}}
\]

参数结果表之后或之前均可。

## 图中内容

横轴：

\[
\log_{10}\beta
\]

纵轴：

\[
\mathrm{CV\ Ordinal\ MAE}.
\]

建议在一张图中画两条曲线：

1. 有序阈值主模型；
2. 单调线性基线。

分别标记：

\[
\widehat\beta_{\mathrm{ord}},
\qquad
\widehat\beta_{\mathrm{lin}}.
\]

## 数据来源

对每一个候选

\[
\beta_q
\]

计算 \(K\)-折交叉验证平均 Ordinal MAE：

\[
CV(\beta_q)
=
\frac1K
\sum_{f=1}^K
\mathrm{MAE}_f(\beta_q).
\]

可进一步用阴影或误差棒表示各折标准差。

## 图注建议

> **图 4 不同 EESM 参数 \(\beta\) 下的交叉验证损失**

## 图的作用

用于说明：

- \(\beta\) 是通过数据选择，而非人为设定；
- 主模型和基线的最优 EESM 参数可能不同；
- 最优点附近曲线形状可以反映模型对 \(\beta\) 的敏感程度。

---

# 六、图 5：主模型与线性基线预测误差分布图（可选）

## 出现位置

建议放在：

\[
\boxed{\text{4.8.3 主模型与基线对比}}
\]

性能比较表之后。

## 图中内容

定义等级预测误差：

\[
e_j
=
c(\widehat y_j)-c(y_j).
\]

横轴：

\[
e_j
\]

例如：

\[
-4,-3,-2,-1,0,1,2,3,4.
\]

纵轴：

\[
\text{样本比例}
\]

或样本数。

分别绘制：

- 有序阈值模型；
- 单调线性模型。

建议采用并列柱状图。

## 图注建议

> **图 5 两种速率映射模型的 MCS 等级预测误差分布**

## 图的作用

用于观察：

- 完全正确预测 \(e=0\) 的比例；
- 错误是否集中在 \(e=\pm1\)；
- 主模型是否比线性基线产生更少的大幅度误判。

---

# 七、图 6：有序阈值主模型混淆矩阵（可选）

## 出现位置

建议放在：

\[
\boxed{\text{4.8.3 主模型与基线对比}}
\]

若正文篇幅有限，可移至附录。

## 图中内容

横轴：

\[
\text{Predicted MCS}
\]

纵轴：

\[
\text{True MCS}.
\]

推荐使用按真实类别逐行归一化的混淆矩阵：

\[
p_{\ell r}
=
P(
\widehat y=m_r
\mid
y=m_\ell
).
\]

## 图注建议

> **图 6 有序阈值模型的归一化 MCS 混淆矩阵**

## 图的作用

若高值主要集中在主对角线及其相邻位置，则说明：

\[
\boxed{
\text{模型错误主要是相邻速率等级之间的局部误判}
}
\]

这可进一步支持 Ordinal MAE 和 \(Acc_{\pm1}\) 的评价结果。

---

# 八、表 1：问题一主要模型参数标定结果

## 出现位置

建议放在：

\[
\boxed{\text{4.8.1 参数标定结果}}
\]

## 表中内容

建议形式：

| 模型 | 最优 EESM 参数 | 第三层模型参数 |
|---|---:|---|
| 单调线性基线 | \(\widehat\beta_{\mathrm{lin}}\) | \(\widehat a,\widehat b\) |
| 有序阈值主模型 | \(\widehat\beta_{\mathrm{ord}}\) | \(\widehat{\boldsymbol\tau}\) |

若阈值数量较多，则表 1 中只写：

\[
\widehat{\boldsymbol\tau}
\text{见表 2}.
\]

例如：

| 模型 | \(\widehat\beta\) | 第三层参数 |
|---|---:|---|
| Linear | 【待填】 | \(a=\)【待填】，\(b=\)【待填】 |
| Ordered Threshold | 【待填】 | 见表 2 |

## 表注建议

> **表 1 问题一两类速率估计模型的最优参数**

---

# 九、表 2：有序阈值模型最优阈值表

## 出现位置

建议紧接：

\[
\boxed{\text{表 1 之后}}
\]

仍属于 4.8.1 参数标定结果。

## 表中内容

| 阈值 | 相邻 MCS 等级 | 最优值 |
|---|---|---:|
| \(\tau_1\) | \(m_0/m_1\) | 【待填】 |
| \(\tau_2\) | \(m_1/m_2\) | 【待填】 |
| \(\vdots\) | \(\vdots\) | \(\vdots\) |
| \(\tau_L\) | \(m_{L-1}/m_L\) | 【待填】 |

若论文展示使用 dB，则可增加一列：

| 阈值 | 线性尺度 | dB |
|---|---:|---:|

但必须明确模型实际计算采用哪一种尺度。

## 表注建议

> **表 2 有序阈值模型各 MCS 等级的最优等效 SINR 分界点**

---

# 十、表 3：主模型与线性基线性能比较

## 出现位置

建议放在：

\[
\boxed{\text{4.8.3 主模型与基线对比}}
\]

这是问题一最重要的结果表之一。

## 表中内容

建议：

| 模型 | Accuracy ↑ | Ordinal MAE ↓ | \(Acc_{\pm1}\) ↑ |
|---|---:|---:|---:|
| 单调线性基线 | 【待填】 | 【待填】 | 【待填】 |
| 有序阈值主模型 | 【待填】 | 【待填】 | 【待填】 |

若采用 \(K\)-折交叉验证，推荐报告：

\[
\boxed{\text{均值}\pm\text{标准差}}
\]

例如：

| 模型 | Accuracy | Ordinal MAE | \(Acc_{\pm1}\) |
|---|---:|---:|---:|
| Linear | \(x\pm s\) | \(x\pm s\) | \(x\pm s\) |
| Ordered Threshold | \(x\pm s\) | \(x\pm s\) | \(x\pm s\) |

## 表注建议

> **表 3 有序阈值主模型与单调线性基线的交叉验证性能比较**

## 表的作用

该表用于回答论文中的核心问题：

\[
\boxed{
\text{有序阈值模型是否确实优于简单线性关系？}
}
\]

---

# 十一、表 4：验证集 B 最终 MCS 预测结果

## 出现位置

建议放在：

\[
\boxed{\text{4.9 验证集 B 的速率预测}}
\]

该表属于题目要求的最终答案，因此原则上必须出现在正文。

## 表中内容

建议：

| 样本编号 | `sta_mac` | `csi_time` | \(SINR_{\mathrm{eff}}\) | 预测 MCS |
|---|---|---|---:|---:|
| 1 | 【待填】 | 【待填】 | 【待填】 | 【待填】 |
| 2 | 【待填】 | 【待填】 | 【待填】 | 【待填】 |
| \(\vdots\) | \(\vdots\) | \(\vdots\) | \(\vdots\) | \(\vdots\) |

如果题目只要求特定信道的预测结果，则正文仅列这些指定样本。

如果 B 集样本很多：

- 正文放题目要求的关键结果；
- 完整预测表放附录。

## 表注建议

> **表 4 验证集 B 中关闭 TxBF 链路的 MCS 预测结果**

---

# 十二、表 5：问题一主要符号说明（可选）

## 出现位置

建议放在：

\[
\boxed{\text{4.2 数据预处理与符号定义}}
\]

如果全文已有统一符号表，则本表可以删除。

## 表中内容

| 符号 | 含义 |
|---|---|
| \(H^{(j)}\) | 第 \(j\) 个样本的 \(2\times4\times122\) CSI |
| \(H_k^{(j)}\) | 第 \(j\) 个样本第 \(k\) 个载波上的信道矩阵 |
| \(P_n^{(j)}\) | 第 \(j\) 个样本的线性噪声功率 |
| \(\gamma_{j,k}\) | 第 \(j\) 个样本第 \(k\) 个载波的 SINR |
| \(z_j\) | 第 \(j\) 个样本的等效 SINR |
| \(\beta\) | EESM 参数 |
| \(m_\ell\) | 第 \(\ell\) 个有序 MCS 等级 |
| \(\tau_\ell\) | 相邻 MCS 等级间的等效 SINR 阈值 |
| \(a,b\) | 单调线性基线参数 |

## 表注建议

> **表 5 问题一主要符号说明**

---

# 十三、表 6：问题一使用的数据字段说明（可选）

## 出现位置

建议放在：

\[
\boxed{\text{4.2 数据预处理}}
\]

## 表中内容

| 数据字段 | 问题一是否使用 | 用途 |
|---|---|---|
| `csi_matrix_r0_c0` ~ `csi_matrix_r1_c3` | 是 | 重构 \(2\times4\times122\) CSI |
| `noise_floor` | 是 | 转换为线性噪声功率并计算 SINR |
| `beamforming_en` | 是 | 筛选 `beamforming_en=0` 样本 |
| `mcs` | 训练集使用 | 监督标签 |
| `csi_time` | 可选 | 样本识别及时间一致性检查 |
| `dfx_time` | 可选 | 检查 CSI 与速率采样时间差 |
| `sta_mac` | 可选 | 区分终端并标识验证集结果 |

## 表注建议

> **表 6 问题一所用原始数据字段及作用**

---

# 十四、正式论文中的推荐排列顺序

建议问题一各图表按照如下顺序出现：

```text
4.1 问题分析
    └─ 图 1  问题一整体建模流程图

4.2 数据预处理与符号定义
    ├─ 表 5  主要符号说明（可选）
    └─ 表 6  数据字段说明（可选）

4.3 载波级 SINR 计算
    └─ 无必须图表

4.4 EESM 等效 SINR 模型
    └─ 无必须图表

4.5 等效 SINR 到 MCS 的速率映射
    └─ 无必须图表

4.6 联合优化与模型求解
    └─ 无必须图表，重点给公式和算法

4.7 模型评价指标
    └─ 无必须图表

4.8 模型求解结果与分析

    4.8.1 参数标定结果
        ├─ 表 1  两模型最优参数
        ├─ 表 2  有序阈值表
        └─ 图 4  β—交叉验证损失曲线

    4.8.2 等效 SINR 与 MCS 的关系
        ├─ 图 2  不同 MCS 的 SINR_eff 箱线图
        └─ 图 3  SINR_eff—MCS 散点及阈值阶梯图

    4.8.3 主模型与基线对比
        ├─ 表 3  模型性能比较
        ├─ 图 5  预测误差分布（可选）
        └─ 图 6  混淆矩阵（可选）

4.9 验证集 B 的速率预测
    └─ 表 4  验证集 B 最终预测结果

4.10 本问小结
```

---

# 十五、推荐最终保留的最精简版本

若整篇论文篇幅较紧，问题一建议至少保留：

## 必须保留的 4 张图

1. **图 1**：问题一整体建模流程图；
2. **图 2**：不同 MCS 的等效 SINR 分布图；
3. **图 3**：等效 SINR—MCS 散点及有序阈值图；
4. **图 4**：\(\beta\)—交叉验证 Ordinal MAE 曲线。

## 必须保留的 4 张表

1. **表 1**：两种模型的参数标定结果；
2. **表 2**：有序阈值参数表；
3. **表 3**：主模型与线性基线性能比较；
4. **表 4**：验证集 B 最终 MCS 预测结果。

若版面允许，再加入：

- 图 5：预测误差分布；
- 图 6：混淆矩阵；
- 表 5：主要符号；
- 表 6：问题一数据字段说明。

---

# 十六、各图表承担的论文论证作用

最终图表并非单纯展示结果，而应形成完整的论证链：

\[
\boxed{
\text{图 1：说明我们怎么建模}
}
\]

\[
\Downarrow
\]

\[
\boxed{
\text{图 4 + 表 1/2：说明参数是怎么确定的}
}
\]

\[
\Downarrow
\]

\[
\boxed{
\text{图 2/3：说明有序阈值模型为什么合理}
}
\]

\[
\Downarrow
\]

\[
\boxed{
\text{表 3：说明主模型为什么优于基线}
}
\]

\[
\Downarrow
\]

\[
\boxed{
\text{表 4：给出题目最终要求的预测结果}
}
\]

这样的安排能够使问题一从“模型提出”到“参数选择”“模型验证”“结果输出”形成完整闭环。
