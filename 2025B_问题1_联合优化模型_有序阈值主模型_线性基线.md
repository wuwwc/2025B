# 2025 年中国研究生数学建模竞赛 B 题——问题 1 联合优化模型（有序阈值主模型 + 线性基线）

## 1. 问题 1 的建模目标

问题 1 针对 **关闭 TxBF** 的 MIMO-OFDM 系统。训练集 A 给出实际信道状态信息、底噪以及实际传输速率索引 `mcs`，验证集 B 仅给出信道信息等输入变量。题目要求完成：

1. 每个载波单元的 SINR 计算；
2. 将所有载波单元的 SINR 映射为等效 SINR；
3. 建立信道传输速率模型，并对验证集 B 的 `mcs` 进行预测。

由于数据集中 **没有提供真实的等效 SINR 标签**，无法先独立拟合 EESM 参数，再单独拟合 `SINR_eff -> MCS`。因此本文采用 **EESM 参数与下游速率映射参数联合优化** 的思路。

第三层速率映射设置两种模型：

- **主模型：有序阈值模型（Ordered Threshold Model）**；
- **基线模型：单调线性模型（Monotone Linear Baseline）**。

完整建模链为

\[
H_{2\times 4\times 122}
\longrightarrow
\{\gamma_k\}_{k=1}^{122}
\longrightarrow
\gamma_{\mathrm{eff}}
\longrightarrow
\widehat{\mathrm{MCS}}.
\]

其中前两层保持一致，仅第三层分别采用有序阈值模型和线性模型进行比较。

---

# 2. 符号与数据定义

设训练集中共有 \(N\) 个满足

\[
\mathrm{beamforming\_en}=0
\]

的样本。第 \(j\) 个样本记为

\[
\left(H^{(j)},P_n^{(j)},y_j\right),
\qquad j=1,\ldots,N,
\]

其中：

- \(H^{(j)}\in\mathbb C^{2\times4\times122}\)：第 \(j\) 个样本的 MIMO-OFDM 信道；
- \(H_k^{(j)}\in\mathbb C^{2\times4}\)：第 \(j\) 个样本第 \(k\) 个载波上的信道矩阵；
- \(K=122\)：载波单元数量；
- \(P_n^{(j)}\)：由 `noise_floor` 转换得到的线性噪声功率；
- \(y_j\)：训练集中的真实传输速率索引 `mcs`。

若 `noise_floor` 的单位为 dBm，则以 mW 为线性功率单位时，

\[
\boxed{
P_n^{(j)}
=
10^{\frac{\mathrm{noise\_floor}_j}{10}}.
}
\]

> 注：赛题给出了 `noise_floor` 的单位为 dBm，但没有进一步明确其与
> \(\|n_k\|^2\) 的逐天线对应关系。因此正式实现时必须统一规定噪声功率定义，
> 并通过数据范围与模型表现核实是否需要乘接收天线数 \(N_r=2\)。

---

# 3. 小问 1：每个载波单元的 SINR 计算

赛题对关闭 TxBF 的 MIMO-OFDM 系统给出的 SINR 形式为

\[
\mathrm{SINR}_k
=
\frac{\|H_k\|^2}{\|n_k\|^2}.
\]

对矩阵信道，采用 Frobenius 范数表示信道总能量：

\[
\left\|H_k^{(j)}\right\|_F^2
=
\sum_{r=0}^{1}
\sum_{c=0}^{3}
\left|h_{r,c,k}^{(j)}\right|^2.
\]

因此，第 \(j\) 个样本第 \(k\) 个载波的线性 SINR 定义为

\[
\boxed{
\gamma_{j,k}
=
\frac{
\left\|H_k^{(j)}\right\|_F^2
}{
P_n^{(j)}
}
},
\qquad
k=1,\ldots,122.
\]

每个样本得到 122 维 SINR 向量

\[
\boxed{
\boldsymbol{\gamma}_j
=
\left(
\gamma_{j,1},
\gamma_{j,2},
\ldots,
\gamma_{j,122}
\right)
\in\mathbb R_+^{122}.
}
\]

### 说明

EESM 指数运算中应使用 **线性 SINR**。若需要画图或解释，可另行计算

\[
\gamma_{j,k}^{(\mathrm{dB})}
=
10\log_{10}\gamma_{j,k},
\]

但不能未经变换直接将 dB 数值代入 EESM 指数项。

---

# 4. 小问 2：EESM 等效 SINR 模型

题目给出一般 ESM 形式

\[
\mathrm{SINR}_{\mathrm{eff}}
=
\alpha
\phi^{-1}
\left[
\frac{1}{K}
\sum_{k=1}^{K}
\phi
\left(
\frac{\mathrm{SINR}_k}{\beta}
\right)
\right].
\]

采用指数信息度量函数

\[
\phi(x)=1-e^{-x},
\]

则

\[
\phi^{-1}(u)
=
-\ln(1-u).
\]

因此第 \(j\) 个样本的等效 SINR 为

\[
\boxed{
z_j(\alpha,\beta)
=
-\alpha
\ln
\left[
\frac{1}{K}
\sum_{k=1}^{K}
\exp
\left(
-\frac{\gamma_{j,k}}{\beta}
\right)
\right]
},
\]

其中

\[
z_j=\mathrm{SINR}_{\mathrm{eff},j},
\qquad
\alpha>0,\quad \beta>0.
\]

---

## 4.1 为什么采用联合优化

训练数据没有提供真实的

\[
\mathrm{SINR}_{\mathrm{eff}}
\]

标签，因此不存在可直接使用的监督目标

\[
\mathrm{SINR}_{\mathrm{eff,true}}
\approx
z_j(\alpha,\beta).
\]

所以 EESM 参数必须通过最终的 MCS 预测效果间接标定，即建立

\[
\boxed{
\text{EESM 参数}
+
\text{速率映射参数}
}
\]

的联合优化模型。

---

## 4.2 \(\alpha\) 的尺度不可辨识性

无论第三层采用有序阈值模型还是线性模型，\(\alpha\) 都只对

\[
z_j
\]

进行统一的正比例缩放。

### 对有序阈值模型

若

\[
z_j'=c z_j,
\qquad
\tau_\ell'=c\tau_\ell,
\]

则 MCS 分类结果不变。

### 对线性模型

若

\[
z_j'=c z_j,
\qquad
a'=\frac{a}{c},
\]

则

\[
a'z_j'+b
=
az_j+b,
\]

预测结果同样不变。

因此，仅依靠 MCS 标签无法同时唯一识别 \(\alpha\) 与第三层尺度参数。为避免参数冗余，固定

\[
\boxed{\alpha=1}.
\]

于是等效 SINR 简化为

\[
\boxed{
z_j(\beta)
=
-
\ln
\left[
\frac{1}{122}
\sum_{k=1}^{122}
\exp
\left(
-\frac{\gamma_{j,k}}{\beta}
\right)
\right],
\qquad \beta>0.
}
\]

后续只需要通过最终 MCS 标签标定 \(\beta\)。

---

# 5. 小问 3：等效 SINR 到 MCS 的速率映射

从训练集中提取全部实际出现的 MCS 值，并按从小到大排列：

\[
m_0<m_1<\cdots<m_L.
\]

定义有序类别编号函数

\[
c(m_\ell)=\ell,
\qquad
\ell=0,\ldots,L.
\]

这样可以避免预先假设 MCS 一定是连续整数。

第三层设置一个主模型和一个基线模型。

---

# 6. 主模型：有序阈值模型

## 6.1 模型结构

引入 \(L\) 个有序阈值：

\[
\tau_1<\tau_2<\cdots<\tau_L,
\]

并约定

\[
\tau_0=-\infty,
\qquad
\tau_{L+1}=+\infty.
\]

根据等效 SINR 所落入的区间预测 MCS：

\[
\boxed{
\widehat y_j^{(\mathrm{ord})}
=
Q_{\boldsymbol\tau}
\left(
z_j(\beta)
\right)
=
m_\ell,
\qquad
\tau_\ell
\le
z_j(\beta)
<
\tau_{\ell+1}.
}
\]

即

\[
\widehat{\mathrm{MCS}}_j^{(\mathrm{ord})}
=
\begin{cases}
m_0,
&z_j<\tau_1,\\[2mm]
m_1,
&\tau_1\le z_j<\tau_2,\\
\vdots\\
m_{L-1},
&\tau_{L-1}\le z_j<\tau_L,\\[2mm]
m_L,
&z_j\ge\tau_L.
\end{cases}
\]

该结构直接体现

\[
\mathrm{SINR}_{\mathrm{eff}}\uparrow
\quad\Longrightarrow\quad
\mathrm{MCS}\uparrow
\]

的通信物理单调性，同时允许不同 MCS 的有效 SINR 区间宽度不同。

---

## 6.2 主模型联合优化目标

有序阈值主模型需要联合估计

\[
\Theta_{\mathrm{ord}}
=
\left(
\beta,\tau_1,\ldots,\tau_L
\right).
\]

由于 MCS 是有序类别，采用类别等级绝对误差：

\[
\ell_j^{(\mathrm{ord})}
=
\left|
c(y_j)
-
c\left(
\widehat y_j^{(\mathrm{ord})}
\right)
\right|.
\]

因此联合优化问题为

\[
\boxed{
\left(
\widehat\beta_{\mathrm{ord}},
\widehat{\boldsymbol\tau}
\right)
=
\arg\min_{
\beta>0,\;
\tau_1<\cdots<\tau_L
}
\frac1N
\sum_{j=1}^{N}
w_{c(y_j)}
\left|
c(y_j)
-
c\left[
Q_{\boldsymbol\tau}
\left(
z_j(\beta)
\right)
\right]
\right|.
}
\]

若类别分布较均衡，可令

\[
w_{c(y_j)}=1.
\]

若类别不平衡明显，可采用

\[
\boxed{
w_\ell
=
\frac{N}{(L+1)N_\ell},
}
\]

其中 \(N_\ell\) 是第 \(\ell\) 个 MCS 类别的样本数。

---

## 6.3 主模型的完整表达式

将前两层代入，有

\[
\boxed{
\widehat y_j^{(\mathrm{ord})}
=
Q_{\boldsymbol\tau}
\left\{
-
\ln
\left[
\frac1{122}
\sum_{k=1}^{122}
\exp
\left(
-
\frac{
\|H_k^{(j)}\|_F^2
}{
\beta P_n^{(j)}
}
\right)
\right]
\right\}.
}
\]

该模型作为问题 1 的 **主方法**。

---

# 7. 基线模型：单调线性模型

## 7.1 设置基线的目的

为了判断有序阈值结构是否确实优于最简单的

\[
\mathrm{SINR}_{\mathrm{eff}}
\rightarrow
\mathrm{MCS}
\]

线性关系，引入线性模型作为基线。

如果线性模型已经能够取得与有序阈值模型接近的预测性能，则说明复杂的分段阈值结构带来的提升有限；反之，如果有序阈值模型显著优于线性模型，则说明 MCS 与等效 SINR 之间具有明显的非线性阶梯特征。

---

## 7.2 线性连续预测

对第 \(j\) 个样本，先计算等效 SINR

\[
z_j(\beta).
\]

然后对 MCS 的类别编号进行线性预测：

\[
\boxed{
\widetilde c_j
=
a\,z_j(\beta)+b,
}
\]

其中

\[
a\ge0
\]

用于保证

\[
\mathrm{SINR}_{\mathrm{eff}}\uparrow
\Rightarrow
\widehat{\mathrm{MCS}}\uparrow
\]

的基本物理单调性。

---

## 7.3 从连续预测恢复离散 MCS

由于真实 MCS 是离散有序类别，需要将

\[
\widetilde c_j
\]

映射回

\[
0,1,\ldots,L.
\]

定义

\[
\boxed{
\widehat c_j^{(\mathrm{lin})}
=
\operatorname{clip}
\left(
\operatorname{round}(\widetilde c_j),
0,L
\right),
}
\]

其中：

- `round` 表示四舍五入到最近整数；
- `clip` 保证预测类别编号不超出 \([0,L]\)。

最终得到

\[
\boxed{
\widehat y_j^{(\mathrm{lin})}
=
m_{\widehat c_j^{(\mathrm{lin})}}.
}
\]

因此线性基线的完整预测结构为

\[
\boxed{
\widehat y_j^{(\mathrm{lin})}
=
m_{
\operatorname{clip}
\left[
\operatorname{round}
\left(
a\,z_j(\beta)+b
\right),
0,L
\right]
}.
}
\]

---

## 7.4 线性基线的联合优化

线性基线同样不能先独立拟合 \(\beta\)，因此联合估计

\[
\Theta_{\mathrm{lin}}
=
(\beta,a,b).
\]

为了保持线性回归基线的标准形式，在连续类别编号上使用加权均方误差：

\[
\boxed{
\left(
\widehat\beta_{\mathrm{lin}},
\widehat a,
\widehat b
\right)
=
\arg\min_{
\beta>0,\;
a\ge0,\;
b\in\mathbb R
}
\frac1N
\sum_{j=1}^{N}
w_{c(y_j)}
\left[
c(y_j)
-
\left(
a\,z_j(\beta)+b
\right)
\right]^2.
}
\]

训练结束后，再通过

\[
\operatorname{round}+\operatorname{clip}
\]

得到最终离散 MCS 预测。

> 说明：线性模型只作为基线，因此不额外引入高阶多项式、样条或其他非线性修正，以保持其“最简单可解释基准”的作用。

---

# 8. 两种第三层模型的统一比较

主模型和基线模型共用相同的前两层：

\[
H
\rightarrow
\{\gamma_k\}_{k=1}^{122}
\rightarrow
z(\beta).
\]

区别只在于第三层：

\[
\boxed{
z
\overset{\text{主模型}}{\longrightarrow}
Q_{\boldsymbol\tau}(z)
}
\]

和

\[
\boxed{
z
\overset{\text{基线}}{\longrightarrow}
az+b.
}
\]

因此可以直接比较：

| 模型 | 第三层形式 | 参数 | 特点 |
|---|---|---|---|
| 线性基线 | \(\widetilde c=az+b\) | \(\beta,a,b\) | 简单、连续、单调 |
| 有序阈值主模型 | \(m_\ell,\ \tau_\ell\le z<\tau_{\ell+1}\) | \(\beta,\tau_1,\ldots,\tau_L\) | 符合 MCS 阶梯式有序结构 |

需要注意：

\[
\widehat\beta_{\mathrm{ord}}
\]

和

\[
\widehat\beta_{\mathrm{lin}}
\]

可以分别通过各自模型的交叉验证目标得到。这样比较的是两套完整模型在同一数据划分上的最佳预测能力。

如需进一步单独比较“第三层映射结构”本身，也可以固定同一组 \(\beta\)，再分别拟合线性模型和有序阈值模型作为辅助实验。

---

# 9. 参数求解方法

## 9.1 主模型：外层搜索 \(\beta\) + 内层阈值优化

由于硬阈值函数不可微，建议使用：

\[
\boxed{
\text{外层搜索 }\beta
+
\text{内层优化 }\boldsymbol\tau.
}
\]

### 外层

在对数尺度设置候选

\[
\beta_1,\beta_2,\ldots,\beta_M>0.
\]

对于每个 \(\beta_q\)，计算

\[
z_j(\beta_q).
\]

### 内层

将

\[
z_1,\ldots,z_N
\]

排序，阈值候选可取相邻不同有效 SINR 的中点：

\[
\mathcal T
=
\left\{
\frac{z_{(i)}+z_{(i+1)}}{2}
\right\}.
\]

在满足

\[
\tau_1<\cdots<\tau_L
\]

的约束下最小化有序 MAE。

可采用：

- 动态规划；
- 坐标搜索；
- 网格搜索配合剪枝。

对于本题的一维等效 SINR，动态规划或坐标搜索均较易实现。

---

## 9.2 线性基线：外层搜索 \(\beta\) + 内层约束线性回归

对于每个候选 \(\beta_q\)：

1. 计算
   \[
   z_j(\beta_q);
   \]
2. 拟合
   \[
   c(y_j)\approx a z_j+b;
   \]
3. 加入
   \[
   a\ge0
   \]
   约束；
4. 计算验证集误差。

选择验证性能最好的

\[
\widehat\beta_{\mathrm{lin}},
\widehat a,
\widehat b.
\]

由于固定 \(\beta\) 后只剩一元线性回归，该基线计算复杂度很低。

---

# 10. 交叉验证

为避免在训练集 A 上过拟合，对两种模型使用 **完全相同的数据划分**进行 \(K_{\mathrm{cv}}\)-折交叉验证。

对主模型：

\[
\mathrm{CVLoss}_{\mathrm{ord}}
=
\frac1{K_{\mathrm{cv}}}
\sum_{q=1}^{K_{\mathrm{cv}}}
L_{q,\mathrm{ord}}.
\]

对线性基线：

\[
\mathrm{CVLoss}_{\mathrm{lin}}
=
\frac1{K_{\mathrm{cv}}}
\sum_{q=1}^{K_{\mathrm{cv}}}
L_{q,\mathrm{lin}}.
\]

所有预处理参数、EESM 参数和第三层模型参数都只能在当前训练折上拟合，再到对应验证折上评价。

---

# 11. 统一评价指标

最终对线性基线和有序阈值主模型报告相同的离散 MCS 指标。

## 11.1 Accuracy

\[
\boxed{
\mathrm{Accuracy}
=
\frac1N
\sum_{j=1}^{N}
\mathbf 1
\left(
\widehat y_j=y_j
\right).
}
\]

---

## 11.2 有序平均绝对误差

\[
\boxed{
\mathrm{MAE}_{\mathrm{ord}}
=
\frac1N
\sum_{j=1}^{N}
\left|
c(\widehat y_j)-c(y_j)
\right|.
}
\]

该指标作为主要比较指标之一，因为预测偏差 1 个 MCS 等级应当比偏差多个等级受到更轻的惩罚。

---

## 11.3 相邻一级准确率

\[
\boxed{
\mathrm{Acc}_{\pm1}
=
\frac1N
\sum_{j=1}^{N}
\mathbf 1
\left(
|c(\widehat y_j)-c(y_j)|\le1
\right).
}
\]

---

## 11.4 可选：混淆矩阵

使用混淆矩阵观察模型主要在哪些相邻 MCS 等级之间发生误判，从而判断误差是否具有明显的局部性。

---

# 12. 主模型与基线的结果判定

建议将交叉验证结果整理为：

| 模型 | Accuracy ↑ | Ordinal MAE ↓ | Acc\(_{\pm1}\) ↑ |
|---|---:|---:|---:|
| 单调线性基线 |  |  |  |
| 有序阈值主模型 |  |  |  |

若有序阈值模型表现出：

\[
\mathrm{Accuracy}_{\mathrm{ord}}
>
\mathrm{Accuracy}_{\mathrm{lin}},
\]

且

\[
\mathrm{MAE}_{\mathrm{ord,threshold}}
<
\mathrm{MAE}_{\mathrm{ord,linear}},
\]

则说明

\[
\mathrm{SINR}_{\mathrm{eff}}
\rightarrow
\mathrm{MCS}
\]

之间的关系更适合用非等距的阶梯阈值描述，而不是简单线性关系。

在问题 1 的最终预测中，原则上采用 **有序阈值模型作为主方法**；线性模型主要承担基线比较和合理性验证作用。

---

# 13. 对验证集 B 的最终预测流程

问题 1 最终使用交叉验证确定的主模型参数

\[
\widehat\beta_{\mathrm{ord}},
\qquad
\widehat{\boldsymbol\tau}.
\]

对验证集 B 中第 \(j\) 个样本：

### Step 1：恢复 122 个 MIMO 信道矩阵

\[
H_1^{(j)},\ldots,H_{122}^{(j)}.
\]

### Step 2：计算各载波 SINR

\[
\gamma_{j,k}
=
\frac{\|H_k^{(j)}\|_F^2}{P_n^{(j)}},
\qquad
k=1,\ldots,122.
\]

### Step 3：计算 EESM 等效 SINR

\[
\boxed{
z_j
=
-
\ln
\left[
\frac1{122}
\sum_{k=1}^{122}
\exp
\left(
-\frac{\gamma_{j,k}}
{\widehat\beta_{\mathrm{ord}}}
\right)
\right].
}
\]

### Step 4：通过最优有序阈值预测 MCS

\[
\boxed{
\widehat y_j
=
Q_{\widehat{\boldsymbol\tau}}(z_j).
}
\]

最终输出验证集 B 的

\[
\boxed{
\widehat{\mathrm{MCS}}_1,
\widehat{\mathrm{MCS}}_2,
\ldots
}
\]

作为问题 1 的预测结果。

---

# 14. 问题 1 三个小问的最终模型总结

## 小问 1：载波级 SINR

\[
\boxed{
\gamma_{j,k}
=
\frac{
\|H_k^{(j)}\|_F^2
}{
P_n^{(j)}
}.
}
\]

输出

\[
\boldsymbol\gamma_j
\in\mathbb R_+^{122}.
\]

---

## 小问 2：EESM 等效 SINR

固定尺度参数

\[
\alpha=1,
\]

得到

\[
\boxed{
z_j(\beta)
=
-
\ln
\left[
\frac1{122}
\sum_{k=1}^{122}
\exp
\left(
-\frac{\gamma_{j,k}}{\beta}
\right)
\right].
}
\]

由于没有等效 SINR 标签，

\[
\beta
\]

必须与第三层模型参数联合标定。

---

## 小问 3：两种速率映射

### 主方法：有序阈值模型

\[
\boxed{
\widehat y_j^{(\mathrm{ord})}
=
Q_{\boldsymbol\tau}
\left[
z_j(\beta)
\right].
}
\]

联合估计

\[
\boxed{
\widehat\beta_{\mathrm{ord}},
\widehat{\tau}_1,
\ldots,
\widehat{\tau}_L.
}
\]

### 基线：单调线性模型

\[
\boxed{
\widetilde c_j
=
a z_j(\beta)+b,
\qquad a\ge0,
}
\]

然后

\[
\boxed{
\widehat c_j^{(\mathrm{lin})}
=
\operatorname{clip}
\left(
\operatorname{round}(\widetilde c_j),
0,L
\right),
\qquad
\widehat y_j^{(\mathrm{lin})}
=
m_{\widehat c_j^{(\mathrm{lin})}}.
}
\]

联合估计

\[
\boxed{
\widehat\beta_{\mathrm{lin}},
\widehat a,
\widehat b.
}
\]

因此，问题 1 的整体比较框架为

\[
\boxed{
H
\rightarrow
\boldsymbol\gamma
\rightarrow
\mathrm{EESM}
\rightarrow
\begin{cases}
\text{有序阈值模型}, & \text{主方法},\\
\text{单调线性模型}, & \text{基线}.
\end{cases}
\rightarrow
\widehat{\mathrm{MCS}}.
}
\]

该设计既保留了题目规定的 EESM 物理层抽象过程，又通过线性基线检验有序阈值结构是否确实带来额外的预测能力。

---

# 15. 正式实现前仍需核实的两个数据问题

## 15.1 `noise_floor` 与 \(\|n_k\|^2\) 的对应关系

赛题给出

\[
\mathrm{SINR}_k
=
\frac{\|H_k\|^2}{\|n_k\|^2},
\]

而数据中只给出标量 `noise_floor`。需要根据实际数据范围判断

\[
P_n
\]

应解释为单接收支路噪声功率还是总噪声功率。

---

## 15.2 CSI 数值尺度

`noise_floor` 使用 dBm，而 CSI 系数的具体物理标定方式在题面中未进一步说明。因此正式计算前应检查 CSI 的数值范围，并确认

\[
\frac{\|H_k\|_F^2}{P_n}
\]

是否得到合理的 SINR 数值。

不能在缺乏依据的情况下对单个样本任意缩放 CSI；所有尺度处理必须保持全数据一致。
