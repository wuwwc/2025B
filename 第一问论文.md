# 第一问论文

> 适用范围：2025 年中国研究生数学建模竞赛 B 题问题 1  
> 本文档依据已经确定的“EESM + 有序阈值主模型 + 单调线性基线”及其求解算法整理。  
> 其中涉及模型运行结果、参数数值和验证集 B 的最终预测值之处，均以 **【待填】** 标记，待代码运行后补充。

---

# 一、第一问论文写作大纲

建议在正式论文中将问题一组织为以下结构：

## 4 问题一：基于 EESM 联合标定的链路速率估计模型

### 4.1 问题分析
说明问题一的输入、输出和主要困难：

- 输入为关闭 TxBF 条件下的 \(2\times4\times122\) MIMO-OFDM 信道、底噪等信息；
- 输出为传输速率索引 MCS；
- 建模过程为“载波级 SINR \(\rightarrow\) 等效 SINR \(\rightarrow\) MCS”；
- 数据中没有真实等效 SINR 标签，因此不能按传统两阶段监督方式独立拟合 EESM 参数；
- 采用 EESM 参数与速率映射参数的联合标定；
- 以有序阈值模型为主模型，以单调线性模型为基线。

### 4.2 数据预处理与符号定义
包括：

- 筛选 `beamforming_en=0`；
- 解析 \(2\times4\times122\) CSI；
- 将 `noise_floor` 从 dBm 转换为线性噪声功率；
- 提取并排序 MCS 类别；
- 定义主要符号。

### 4.3 载波级 SINR 计算
由每个载波的信道矩阵 \(H_k\) 计算：

\[
\gamma_{j,k}
=
\frac{\|H_k^{(j)}\|_F^2}{P_n^{(j)}}.
\]

### 4.4 EESM 等效 SINR 模型
给出指数型 ESM：

\[
z_j(\beta)
=
-\ln
\left[
\frac1{122}
\sum_{k=1}^{122}
\exp\left(-\frac{\gamma_{j,k}}{\beta}\right)
\right].
\]

并解释：

- 为什么采用联合优化；
- 为什么固定 \(\alpha=1\)；
- \(\beta\) 如何通过最终 MCS 预测效果间接标定。

### 4.5 等效 SINR 到 MCS 的速率映射
分成两个模型：

#### 4.5.1 有序阈值主模型
建立一组单调阈值：

\[
\tau_1<\cdots<\tau_L,
\]

根据等效 SINR 所处区间预测 MCS。

#### 4.5.2 单调线性基线
建立：

\[
\widetilde c_j=a z_j+b,\qquad a\ge0,
\]

再通过 `round + clip` 得到离散 MCS。

### 4.6 联合优化与模型求解

#### 4.6.1 主模型
采用：

\[
\text{外层对数网格搜索 }\beta
+
\text{内层动态规划求最优阈值}.
\]

#### 4.6.2 线性基线
采用：

\[
\text{外层对数网格搜索 }\beta
+
\text{内层加权最小二乘解析解}.
\]

#### 4.6.3 交叉验证
采用相同的 \(K\)-折数据划分，以 Ordinal MAE 作为外层选择 \(\beta\) 的主要指标。

### 4.7 模型评价指标
建议报告：

- Accuracy；
- Ordinal MAE；
- \(\mathrm{Acc}_{\pm1}\)；
- 可选：混淆矩阵。

### 4.8 结果与分析
建议至少包含：

- 最优参数表；
- 主模型与基线交叉验证结果对比表；
- 等效 SINR—MCS 散点图/箱线图及阈值；
- \(\beta\)—CV Loss 曲线；
- 模型优劣分析。

### 4.9 验证集 B 的预测
使用最终主模型对 B 预测，并给出题目要求的 MCS 结果表。

### 4.10 本问小结
简洁总结 EESM 降维、联合标定和有序阈值建模的作用及结果。

---

# 二、第一问论文正文草稿

# 4 问题一：基于 EESM 联合标定的链路速率估计模型

## 4.1 问题分析

问题一要求利用完备数据集 A 中关闭发送波束赋形（TxBF）时的 MIMO-OFDM 信道状态信息和实际传输速率标签，对 EESM 模型进行参数标定，并进一步预测不完备数据集 B 中对应信道的传输速率索引。

对于第 \(j\) 个样本，原始信道信息可表示为

\[
H^{(j)}
=
\left[
H_1^{(j)},H_2^{(j)},\ldots,H_{122}^{(j)}
\right],
\]

其中每个载波单元上的信道矩阵均满足

\[
H_k^{(j)}\in\mathbb C^{2\times4}.
\]

因此，问题一的物理层抽象过程可概括为

\[
\boxed{
H_{2\times4\times122}
\longrightarrow
\{\gamma_k\}_{k=1}^{122}
\longrightarrow
\gamma_{\mathrm{eff}}
\longrightarrow
\widehat{\mathrm{MCS}}
}.
\]

首先，根据各载波的 MIMO 信道矩阵及底噪计算载波级 SINR；随后利用指数型等效 SINR 映射（EESM）将 122 维 SINR 向量压缩为一个标量等效 SINR；最后建立等效 SINR 与实际 MCS 之间的映射关系。

该问题的主要困难在于，训练数据仅给出了最终 MCS 标签，并未提供真实的等效 SINR。因而无法先利用等效 SINR 标签独立标定 EESM 参数，再进行速率映射拟合。为此，本文将 EESM 参数和下游速率映射参数视为一个整体，直接以最终 MCS 预测误差作为标定依据，构建联合优化模型。

考虑到 MCS 本质上具有明确的等级顺序，本文以 **有序阈值模型** 作为主要速率映射方法，使模型显式保持“等效 SINR 越大，支持的 MCS 等级越高”的单调结构；同时建立 **单调线性模型** 作为基线，以检验分段阈值结构相对于简单线性关系是否具有实际增益。

---

## 4.2 数据预处理与符号定义

### 4.2.1 样本筛选

问题一研究关闭 TxBF 的链路速率，因此首先从训练数据 A 中筛选

\[
\mathrm{beamforming\_en}=0
\]

的样本。设筛选后共有 \(N\) 条训练记录。

### 4.2.2 CSI 重构

数据表中 CSI 被拆分存储在

\[
\texttt{csi\_matrix\_r0\_c0}
\sim
\texttt{csi\_matrix\_r1\_c3}
\]

共 8 列中，每个单元包含 122 个载波单元的复信道系数。将其按照接收天线、发送天线和载波三个维度重新组合，可得到

\[
H^{(j)}\in\mathbb C^{2\times4\times122}.
\]

记第 \(j\) 个样本在第 \(k\) 个载波上的信道矩阵为

\[
H_k^{(j)}\in\mathbb C^{2\times4}.
\]

### 4.2.3 底噪转换

数据中的 `noise_floor` 以 dBm 为单位。以 mW 为线性功率单位时，定义第 \(j\) 个样本的噪声功率为

\[
\boxed{
P_n^{(j)}
=
10^{\mathrm{noise\_floor}_j/10}.
}
\]

由于题面仅给出标量底噪，未进一步说明其与多接收支路噪声范数之间的具体归一化方式，因此本文在全体样本中采用统一噪声功率口径，并在数值实验中检查所得 SINR 的数量级与稳定性。

### 4.2.4 MCS 有序编码

设训练集中实际出现的 MCS 取值按从小到大排列为

\[
m_0<m_1<\cdots<m_L.
\]

定义类别编号函数

\[
c(m_\ell)=\ell,
\qquad
\ell=0,1,\ldots,L.
\]

该编码仅用于优化和误差度量，最终输出仍恢复为原始 MCS 索引。

---

## 4.3 载波级 SINR 计算

题目给出的关闭 TxBF 的 MIMO-OFDM 系统载波级 SINR 为

\[
\mathrm{SINR}_k
=
\frac{\|H_k\|^2}{\|n_k\|^2}.
\]

对于第 \(j\) 个样本的第 \(k\) 个载波，使用 Frobenius 范数刻画 MIMO 信道矩阵的总能量：

\[
\left\|H_k^{(j)}\right\|_F^2
=
\sum_{r=0}^{1}
\sum_{c=0}^{3}
\left|h_{r,c,k}^{(j)}\right|^2.
\]

于是定义

\[
\boxed{
\gamma_{j,k}
=
\frac{
\left\|H_k^{(j)}\right\|_F^2
}{
P_n^{(j)}
},
\qquad
k=1,\ldots,122.
}
\]

第 \(j\) 个样本可表示为一个 122 维载波级 SINR 向量：

\[
\boldsymbol\gamma_j
=
\left(
\gamma_{j,1},
\ldots,
\gamma_{j,122}
\right).
\]

需要指出的是，后续 EESM 中的指数映射采用线性 SINR。若为绘图和结果解释需要，可另行转换为 dB：

\[
\gamma_{j,k}^{(\mathrm{dB})}
=
10\log_{10}\gamma_{j,k}.
\]

---

## 4.4 EESM 等效 SINR 模型

EESM 的作用是将不同载波上的信道质量压缩为一个能够反映整体链路状态的标量。题目给出的一般 ESM 形式为

\[
\mathrm{SINR}_{\mathrm{eff}}
=
\alpha
\phi^{-1}
\left[
\frac1K
\sum_{k=1}^{K}
\phi
\left(
\frac{\mathrm{SINR}_k}{\beta}
\right)
\right].
\]

对于指数信息度量

\[
\phi(x)=1-e^{-x},
\]

有

\[
\phi^{-1}(u)=-\ln(1-u).
\]

因此第 \(j\) 个样本的等效 SINR 可写为

\[
z_j(\alpha,\beta)
=
-\alpha
\ln
\left[
\frac1{122}
\sum_{k=1}^{122}
\exp
\left(
-\frac{\gamma_{j,k}}{\beta}
\right)
\right].
\]

其中 \(\alpha,\beta>0\) 为模型参数。

### 4.4.1 参数可辨识性与尺度归一化

由于训练集中没有真实等效 SINR 标签，EESM 参数只能通过最终 MCS 预测误差间接确定。进一步地，参数 \(\alpha\) 只对全部等效 SINR 进行统一正比例缩放。

对于有序阈值模型，若令

\[
z_j'=qz_j,\qquad
\tau_\ell'=q\tau_\ell,
\]

其中 \(q>0\)，则所有样本的 MCS 分类结果保持不变。对于线性基线，只需同时令

\[
a'=\frac{a}{q},
\]

也可保持

\[
a'z_j'+b=az_j+b.
\]

因此，仅依赖 MCS 标签时，\(\alpha\) 与第三层速率映射中的尺度参数无法分别唯一识别。为消除这一尺度冗余，本文固定

\[
\boxed{\alpha=1}.
\]

EESM 因而简化为

\[
\boxed{
z_j(\beta)
=
-\ln
\left[
\frac1{122}
\sum_{k=1}^{122}
\exp
\left(
-\frac{\gamma_{j,k}}{\beta}
\right)
\right],
\qquad \beta>0.
}
\]

参数 \(\beta\) 不再单独拟合，而与后续速率映射参数共同标定。

---

## 4.5 等效 SINR 到 MCS 的速率映射

### 4.5.1 主模型：有序阈值模型

MCS 具有天然的等级结构，因此设置 \(L\) 个有序阈值

\[
\tau_1<\tau_2<\cdots<\tau_L,
\]

并定义

\[
\tau_0=-\infty,
\qquad
\tau_{L+1}=+\infty.
\]

若第 \(j\) 个样本的等效 SINR 满足

\[
\tau_\ell
\le
z_j(\beta)
<
\tau_{\ell+1},
\]

则预测为第 \(\ell\) 个 MCS 等级，即

\[
\boxed{
\widehat y_j^{(\mathrm{ord})}
=
Q_{\boldsymbol\tau}
\left(
z_j(\beta)
\right)
=
m_\ell.
}
\]

因此有

\[
\widehat y_j^{(\mathrm{ord})}
=
\begin{cases}
m_0, & z_j<\tau_1,\\
m_1, & \tau_1\le z_j<\tau_2,\\
\vdots\\
m_{L-1}, & \tau_{L-1}\le z_j<\tau_L,\\
m_L, & z_j\ge\tau_L.
\end{cases}
\]

该模型允许不同 MCS 等级拥有不同宽度的有效 SINR 区间，比等距线性映射更能表示实际链路自适应中的阶梯式速率变化。

为体现 MCS 的有序性，定义第 \(j\) 个样本的等级误差为

\[
\ell_j
=
\left|
c(y_j)
-
c\left(\widehat y_j^{(\mathrm{ord})}\right)
\right|.
\]

若不同 MCS 类别样本数差异较大，则令

\[
w_\ell
=
\frac{N}{(L+1)N_\ell},
\]

其中 \(N_\ell\) 为类别 \(m_\ell\) 的样本数；若类别分布较均衡，则取 \(w_\ell=1\)。

于是主模型的联合优化问题为

\[
\boxed{
\left(
\widehat\beta_{\mathrm{ord}},
\widehat{\boldsymbol\tau}
\right)
=
\arg\min_{
\beta>0,\;
\tau_1<\cdots<\tau_L
}
\frac1N
\sum_{j=1}^{N}
w_{c(y_j)}
\left|
c(y_j)
-
c\left[
Q_{\boldsymbol\tau}
\left(
z_j(\beta)
\right)
\right]
\right|.
}
\]

将载波级 SINR 与 EESM 代入后，完整预测模型为

\[
\boxed{
\widehat y_j^{(\mathrm{ord})}
=
Q_{\boldsymbol\tau}
\left\{
-\ln
\left[
\frac1{122}
\sum_{k=1}^{122}
\exp
\left(
-\frac{\|H_k^{(j)}\|_F^2}
{\beta P_n^{(j)}}
\right)
\right]
\right\}.
}
\]

---

### 4.5.2 基线模型：单调线性模型

为检验有序阈值结构是否确有必要，建立简单的单调线性模型作为基线。令

\[
\boxed{
\widetilde c_j
=
a z_j(\beta)+b,
\qquad a\ge0,
}
\]

其中 \(a\ge0\) 保证等效 SINR 增大时预测 MCS 不下降。

由于 \(\widetilde c_j\) 为连续值，将其映射回离散类别编号：

\[
\widehat c_j^{(\mathrm{lin})}
=
\operatorname{clip}
\left[
\operatorname{round}
\left(
\widetilde c_j
\right),
0,L
\right].
\]

最终：

\[
\boxed{
\widehat y_j^{(\mathrm{lin})}
=
m_{\widehat c_j^{(\mathrm{lin})}}.
}
\]

线性基线通过连续类别编号上的加权最小二乘进行联合标定：

\[
\boxed{
\left(
\widehat\beta_{\mathrm{lin}},
\widehat a,
\widehat b
\right)
=
\arg\min_{
\beta>0,\;
a\ge0,\;
b\in\mathbb R
}
\frac1N
\sum_{j=1}^{N}
w_{c(y_j)}
\left[
c(y_j)
-
a z_j(\beta)
-
b
\right]^2.
}
\]

主模型与基线共用相同的载波级 SINR 计算和 EESM 结构，因此二者的差异主要来自等效 SINR 到 MCS 的第三层映射，可据此判断非等距阶梯结构是否优于简单线性关系。

---

## 4.6 联合优化与模型求解

### 4.6.1 总体求解思路

主模型的目标函数含有阶梯映射 \(Q_{\boldsymbol\tau}\)，整体属于带有序约束的非线性、非光滑优化问题；线性基线由于 EESM 参数 \(\beta\) 的存在，整体属于非线性最小二乘问题。

但两类模型均只有一个 EESM 参数 \(\beta\)。固定 \(\beta\) 后：

- 有序阈值模型转化为一维有序分割问题；
- 线性基线转化为带 \(a\ge0\) 约束的凸加权线性回归。

因此采用剖面优化思想，将联合优化统一分解为

\[
\boxed{
\text{外层一维搜索 }\beta
+
\text{内层求最优速率映射参数}.
}
\]

由于 \(\beta>0\)，在 \(\log\beta\) 尺度上进行“粗搜索 + 局部细搜索”，既能保证参数正性，又可覆盖多个数量级。

---

### 4.6.2 有序阈值主模型的动态规划求解

固定某个候选 \(\beta\) 后，首先计算全部训练样本的

\[
z_j(\beta),
\]

并按从小到大排序：

\[
z_{(1)}
\le
z_{(2)}
\le
\cdots
\le
z_{(N)}.
\]

由于预测 MCS 随等效 SINR 单调增加，阈值只需位于相邻不同等效 SINR 之间。可将候选阈值写为

\[
t_i
=
\frac{
z_{(i)}+z_{(i+1)}
}{2}.
\]

若将排序后的第 \(i\) 至第 \(r\) 个样本统一预测为类别 \(\ell\)，定义分段代价

\[
\boxed{
C(i,r,\ell)
=
\sum_{s=i}^{r}
w_{c(y_{(s)})}
\left|
c(y_{(s)})-\ell
\right|.
}
\]

定义动态规划状态

\[
DP(\ell,i)
\]

为“将前 \(i\) 个排序样本划分到类别 \(0,1,\ldots,\ell\) 时的最小累计误差”。

初始状态为

\[
DP(0,i)
=
C(1,i,0),
\]

状态转移为

\[
\boxed{
DP(\ell,i)
=
\min_{r<i}
\left\{
DP(\ell-1,r)
+
C(r+1,i,\ell)
\right\}.
}
\]

记录每一步取得最小值的切分位置 \(r^*(\ell,i)\)，从 \(DP(L,N)\) 反向回溯即可得到各等级的最优切分位置，并进一步求得

\[
\boxed{
\tau_\ell
=
\frac{
z_{(r_\ell)}
+
z_{(r_\ell+1)}
}{2}.
}
\]

由此得到固定 \(\beta\) 下的全局最优阈值

\[
\boldsymbol\tau^*(\beta).
\]

---

### 4.6.3 线性基线的解析求解

固定 \(\beta\) 后，记

\[
z_j=z_j(\beta).
\]

定义加权均值

\[
\bar z_w
=
\frac{\sum_j w_jz_j}{\sum_jw_j},
\qquad
\bar c_w
=
\frac{\sum_jw_jc(y_j)}{\sum_jw_j}.
\]

无约束加权最小二乘斜率为

\[
a_0
=
\frac{
\sum_jw_j
(z_j-\bar z_w)
(c(y_j)-\bar c_w)
}{
\sum_jw_j
(z_j-\bar z_w)^2
}.
\]

结合单调约束 \(a\ge0\)，有

\[
\boxed{
\widehat a(\beta)=\max(0,a_0),
}
\]

以及

\[
\boxed{
\widehat b(\beta)
=
\bar c_w
-
\widehat a(\beta)\bar z_w.
}
\]

因此固定 \(\beta\) 后，线性基线无需使用迭代优化器即可得到解析最优解。

---

### 4.6.4 外层搜索与交叉验证

为防止在训练集 A 上过拟合，对两个模型采用完全相同的 \(K_{\mathrm{cv}}\)-折交叉验证划分。

对于每个候选 \(\beta_q\)：

1. 仅在当前训练折上拟合第三层模型参数；
2. 在当前验证折上预测 MCS；
3. 计算验证折 Ordinal MAE；
4. 对所有折取平均。

主模型的交叉验证损失为

\[
CV_{\mathrm{ord}}(\beta)
=
\frac1{K_{\mathrm{cv}}}
\sum_{f=1}^{K_{\mathrm{cv}}}
\mathrm{MAE}_{f,\mathrm{ord}}(\beta),
\]

线性基线为

\[
CV_{\mathrm{lin}}(\beta)
=
\frac1{K_{\mathrm{cv}}}
\sum_{f=1}^{K_{\mathrm{cv}}}
\mathrm{MAE}_{f,\mathrm{lin}}(\beta).
\]

分别选取

\[
\boxed{
\widehat\beta_{\mathrm{ord}}
=
\arg\min_\beta
CV_{\mathrm{ord}}(\beta),
}
\]

以及

\[
\boxed{
\widehat\beta_{\mathrm{lin}}
=
\arg\min_\beta
CV_{\mathrm{lin}}(\beta).
}
\]

确定最优 \(\beta\) 后，再利用全部训练集 A 重新拟合最终阈值或线性系数。

为避免指数计算中的数值下溢，EESM 在程序中使用 `logsumexp` 实现：

\[
z_j
=
-
\left[
\operatorname{logsumexp}
\left(
-\frac{\gamma_{j,1}}{\beta},
\ldots,
-\frac{\gamma_{j,122}}{\beta}
\right)
-\ln122
\right].
\]

---

## 4.7 模型评价指标

考虑到 MCS 同时具有“离散类别”和“有序等级”两种属性，采用以下三个指标统一评价主模型与基线模型。

### 4.7.1 准确率

\[
\boxed{
\mathrm{Accuracy}
=
\frac1N
\sum_{j=1}^{N}
\mathbf 1
\left(
\widehat y_j=y_j
\right).
}
\]

该指标衡量完全预测正确的样本比例。

### 4.7.2 有序平均绝对误差

\[
\boxed{
\mathrm{MAE}_{\mathrm{ord}}
=
\frac1N
\sum_{j=1}^{N}
\left|
c(\widehat y_j)-c(y_j)
\right|.
}
\]

与普通分类错误率相比，该指标能够区分“相差一个 MCS 等级”和“相差多个 MCS 等级”的误差程度，因此将其作为外层参数选择的主要评价标准。

### 4.7.3 相邻一级准确率

\[
\boxed{
\mathrm{Acc}_{\pm1}
=
\frac1N
\sum_{j=1}^{N}
\mathbf 1
\left(
|c(\widehat y_j)-c(y_j)|\le1
\right).
}
\]

该指标用于衡量预测结果是否落在真实 MCS 及其相邻等级范围内。

---

## 4.8 模型求解结果与分析

> 本节中的具体数值需在代码运行后填写，不能在建模阶段预设。

### 4.8.1 参数标定结果

建议将最终参数整理为表 1。

**表 1  问题一模型参数标定结果**

| 模型 | EESM 参数 | 第三层参数 |
|---|---:|---|
| 单调线性基线 | \(\widehat\beta_{\mathrm{lin}}=\)【待填】 | \(\widehat a=\)【待填】，\(\widehat b=\)【待填】 |
| 有序阈值主模型 | \(\widehat\beta_{\mathrm{ord}}=\)【待填】 | \(\widehat\tau_1,\ldots,\widehat\tau_L=\)【待填】 |

建议绘制 **图 1：交叉验证损失随 \(\log\beta\) 的变化曲线**，以展示外层参数搜索结果及最优 \(\beta\) 的稳定区域。

> **【图 1 占位】**  
> 横轴：\(\log\beta\)；纵轴：交叉验证 Ordinal MAE。  
> 分别绘制有序阈值主模型和单调线性基线。

---

### 4.8.2 等效 SINR 与 MCS 的关系

利用主模型的最优参数计算训练样本的等效 SINR，并绘制等效 SINR 与真实 MCS 的关系图。

建议使用：

- MCS 分组箱线图或小提琴图；
- 等效 SINR—MCS 抖动散点图；
- 在图中叠加最优阈值 \(\widehat\tau_\ell\)。

> **【图 2 占位】**  
> 等效 SINR 与 MCS 的分布关系及最优有序阈值。

若图中各 MCS 等级的等效 SINR 整体随 MCS 增大而右移，且相邻类别之间存在不同宽度的过渡区间，则说明“等效 SINR 与 MCS 单调相关但并非简单等距线性”的建模假设具有合理性。

---

### 4.8.3 主模型与基线对比

将交叉验证结果整理为表 2。

**表 2  两种速率映射模型的交叉验证性能**

| 模型 | Accuracy ↑ | Ordinal MAE ↓ | \(\mathrm{Acc}_{\pm1}\) ↑ |
|---|---:|---:|---:|
| 单调线性基线 | 【待填】 | 【待填】 | 【待填】 |
| 有序阈值主模型 | 【待填】 | 【待填】 | 【待填】 |

结果分析可按照实际运行情况写成以下两种情形之一：

**若主模型明显优于基线：**

> 从表 2 可见，有序阈值模型在 Accuracy 和 \(\mathrm{Acc}_{\pm1}\) 上均高于线性基线，同时取得更低的 Ordinal MAE。这表明不同 MCS 等级对应的等效 SINR 区间并非等距分布，采用非等距有序阈值能够更准确地刻画实际链路的阶梯式速率变化。因此后续对验证集 B 的预测采用有序阈值模型。

**若二者性能接近：**

> 从表 2 可见，两种模型的预测性能较为接近，说明等效 SINR 与 MCS 在当前数据范围内已经表现出较强的近似单调线性关系。考虑到有序阈值模型能够直接反映 MCS 的离散等级机制，且不会牺牲单调性，本文仍将其作为问题一的主要模型，并以线性模型作为合理性基准。

还可进一步绘制混淆矩阵，分析误差是否主要集中于相邻 MCS 等级。

---

## 4.9 验证集 B 的速率预测

利用训练集 A 的全部关闭 TxBF 样本，以交叉验证确定的

\[
\widehat\beta_{\mathrm{ord}}
\]

重新拟合最优有序阈值

\[
\widehat{\boldsymbol\tau}.
\]

对于验证集 B 中第 \(j\) 个待预测样本，首先计算

\[
\gamma_{j,k}
=
\frac{\|H_k^{(j)}\|_F^2}{P_n^{(j)}},
\qquad
k=1,\ldots,122,
\]

再计算等效 SINR

\[
\boxed{
z_j
=
-\ln
\left[
\frac1{122}
\sum_{k=1}^{122}
\exp
\left(
-\frac{\gamma_{j,k}}
{\widehat\beta_{\mathrm{ord}}}
\right)
\right].
}
\]

最终根据

\[
\widehat y_j
=
Q_{\widehat{\boldsymbol\tau}}(z_j)
\]

得到预测 MCS。

建议按照题目要求将最终结果列成表 3。

**表 3  验证集 B 的问题一 MCS 预测结果**

| 样本编号/终端标识 | 预测等效 SINR | 预测 MCS |
|---|---:|---:|
| 【待填】 | 【待填】 | 【待填】 |
| 【待填】 | 【待填】 | 【待填】 |
| \(\vdots\) | \(\vdots\) | \(\vdots\) |

若题目只要求填写指定信道的速率估计，则正文仅列出指定样本；完整结果可放入附录。

---

## 4.10 本问小结

针对关闭 TxBF 的 MIMO-OFDM 链路，本文首先依据 CSI 和底噪信息计算 122 个载波单元的 SINR，再利用 EESM 将高维载波状态压缩为单一等效 SINR。由于数据中缺少真实等效 SINR 标签，本文没有采用相互独立的两阶段参数拟合，而是直接以最终 MCS 预测误差为目标，对 EESM 参数和速率映射参数进行联合标定。

在速率映射层，本文根据 MCS 的有序离散特征建立非等距有序阈值模型，并设置单调线性模型作为基线。求解时，将原联合优化分解为一维 EESM 参数搜索和内层参数求解：主模型使用动态规划获得固定 \(\beta\) 下的最优阈值，线性基线使用加权最小二乘解析求解。随后利用 \(K\)-折交叉验证选择 EESM 参数，并通过 Accuracy、Ordinal MAE 和相邻一级准确率比较两种模型。

最终将根据实际数值结果确定问题一的最优模型参数，并利用主模型完成验证集 B 中指定链路的 MCS 预测。

---

# 三、建议正式论文中保留的图表

为使问题一部分结构完整、结果具有说服力，建议最终至少保留下列图表：

1. **问题一建模流程图**  
   \[
   CSI\rightarrow 122\text{ 个 SINR}\rightarrow EESM
   \rightarrow
   \begin{cases}
   \text{有序阈值}\\
   \text{单调线性}
   \end{cases}
   \rightarrow MCS
   \]

2. **\(\beta\)-交叉验证损失曲线**  
   用于说明 EESM 参数的选择。

3. **等效 SINR—MCS 分布图及最优阈值**  
   用于验证有序阈值模型的合理性。

4. **主模型与基线的指标对比表**  
   Accuracy、Ordinal MAE、\(\mathrm{Acc}_{\pm1}\)。

5. **验证集 B 的最终预测结果表**  
   这是题目明确要求写入正文的结果。

如篇幅允许，可增加混淆矩阵，用于分析模型误差是否主要发生在相邻 MCS 等级之间。

---

# 四、参考文献引用建议

问题一中 EESM 与物理层抽象的背景可引用赛题给出的文献：

[1] Brueninghaus K., et al. Link performance models for system level simulations of broadband radio access systems. *IEEE PIMRC*, 2005.

[2] Hoefel R. P. F., Bejarano O. On application of PHY layer abstraction techniques for system level simulation and adaptive modulation in IEEE 802.11ac/ax systems, 2016.

[3] Hanzaz Z., Schotten H. D. Analysis of effective SINR mapping models for MIMO OFDM in LTE system. *IWCMC*, 2013.

[4] Tsai S. S., Soong A. Effective-SNR mapping for modeling frame error rates in multiple-state channels. 3GPP2 Technical Report, 2003.

正式排版时可根据全文参考文献编号统一调整。
